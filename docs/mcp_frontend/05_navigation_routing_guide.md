# 🧭 ZER0 WASTE AI - NAVIGATION & ROUTING GUIDE\n\n## 📋 **GOROUTER NAVIGATION SYSTEM**\n\nZER0 Waste AI uses **GoRouter** for declarative routing with advanced features like guards, nested navigation, and deep linking support.\n\n---\n\n## 🏗️ **ROUTING ARCHITECTURE**\n\n### **Router Structure Overview**\n\n```\n🌐 Root Navigator (_rootNavigatorKey)\n├── 🚀 Splash & Onboarding\n├── 🔐 Authentication Flow\n├── ⚙️ Preference Setup Flow\n├── 📱 Shell Navigator (_shellNavigatorKey)\n│   ├── 🏠 Home Tab\n│   ├── 📦 Inventory Tab\n│   ├── 🍳 Recipes Tab\n│   └── 👤 Profile Tab\n├── 🎯 Feature-Specific Flows\n└── 📄 Information Pages\n```\n\n---\n\n## 📍 **ROUTE DEFINITIONS**\n\n**Location:** `lib/core/navigation/app_router.dart`\n\n### **Route Constants**\n\n```dart\n// Authentication Routes\nconst String splashRouteName = 'splash';\nconst String onboardingRouteName = 'onboarding';\nconst String loginRouteName = 'login';\nconst String registerRouteName = 'register';\nconst String forgotPasswordRouteName = 'forgot-password';\n\n// Main Navigation Routes\nconst String homeRouteName = 'home';\nconst String inventoryRouteName = 'inventory';\nconst String recipesRouteName = 'recipes';\nconst String profileRouteName = 'profile';\n\n// Feature Routes\nconst String smartRecipesRouteName = 'smart-recipes';\nconst String impactRouteName = 'impact';\nconst String plannerRouteName = 'planner';\n\n// Preference Setup Routes\nconst String allergySelectorRouteName = AllergySelectorScreen.routeName;\nconst String cookingLevelSelectorRouteName = CookingLevelSelectorScreen.routeName;\nconst String preferredFoodTypeRouteName = PreferredFoodTypeScreen.routeName;\nconst String specialDietSelectorRouteName = SpecialDietSelectorScreen.routeName;\n```\n\n---\n\n## 🔐 **AUTHENTICATION FLOW**\n\n### **Auth Route Structure**\n\n```dart\n// Splash Screen - App initialization\nGoRoute(\n  path: '/splash',\n  name: splashRouteName,\n  builder: (context, state) => const SplashScreen(),\n),\n\n// Onboarding - First-time user introduction\nGoRoute(\n  path: '/onboarding',\n  name: onboardingRouteName,\n  builder: (context, state) => const OnboardingScreen(),\n),\n\n// Login Screen\nGoRoute(\n  path: '/login',\n  name: loginRouteName,\n  builder: (context, state) => const LoginScreen(),\n),\n\n// Registration Screen\nGoRoute(\n  path: '/register',\n  name: registerRouteName,\n  builder: (context, state) => const RegisterScreen(),\n),\n\n// Password Recovery\nGoRoute(\n  path: '/forgot-password',\n  name: forgotPasswordRouteName,\n  builder: (context, state) => const ForgotPasswordScreen(),\n),\n```\n\n### **Authentication Guard Logic**\n\n```dart\nredirect: (context, state) {\n  final isLoggedIn = authState.value != null;\n  final user = authState.value;\n  final userProfileState = ref.watch(userProfileProvider);\n  final hasCompletedPreferences = userProfileState.user?.initialPreferencesCompleted ?? false;\n  \n  // Route analysis\n  final isGoingToLogin = state.matchedLocation == '/login';\n  final isGoingToRegister = state.matchedLocation == '/register';\n  final isGoingToAuthFlow = state.matchedLocation.startsWith('/allergy-selector') ||\n                          state.matchedLocation.startsWith('/cooking-level-selector') ||\n                          state.matchedLocation.startsWith('/preferred-food-type') ||\n                          state.matchedLocation.startsWith('/special-diet-selector');\n  \n  // Redirect logic\n  if (!isLoggedIn && !isGoingToLogin && !isGoingToRegister && !isGoingToAuthFlow) {\n    return '/login'; // Redirect unauthenticated users\n  }\n  \n  if (isLoggedIn && user != null) {\n    if (hasCompletedPreferences && isGoingToAuthFlow) {\n      return '/home'; // Redirect completed users away from setup\n    }\n    \n    if (!hasCompletedPreferences && !isGoingToAuthFlow) {\n      return '/allergy-selector'; // Force preference completion\n    }\n  }\n  \n  return null; // No redirect needed\n},\n```\n\n---\n\n## ⚙️ **PREFERENCE SETUP FLOW**\n\n### **Sequential Preference Routes**\n\n```dart\n// Step 1: Allergy Selection\nGoRoute(\n  path: AllergySelectorScreen.routePath, // '/allergy-selector'\n  name: allergySelectorRouteName,\n  builder: (context, state) => const AllergySelectorScreen(),\n),\n\n// Step 2: Cooking Level Selection\nGoRoute(\n  path: CookingLevelSelectorScreen.routePath, // '/cooking-level-selector'\n  name: cookingLevelSelectorRouteName,\n  builder: (context, state) => const CookingLevelSelectorScreen(),\n),\n\n// Step 3: Food Type Preferences\nGoRoute(\n  path: PreferredFoodTypeScreen.routePath, // '/preferred-food-type'\n  name: preferredFoodTypeRouteName,\n  builder: (context, state) => const PreferredFoodTypeScreen(),\n),\n\n// Step 4: Special Diet Selection\nGoRoute(\n  path: SpecialDietSelectorScreen.routePath, // '/special-diet-selector'\n  name: specialDietSelectorRouteName,\n  builder: (context, state) => const SpecialDietSelectorScreen(),\n),\n```\n\n### **Preference Flow Navigation**\n\n```dart\n// Navigation pattern in preference screens\nclass AllergySelectorScreen extends ConsumerWidget {\n  static const String routePath = '/allergy-selector';\n  static const String routeName = 'allergy-selector';\n  \n  void _navigateToNext(BuildContext context) {\n    context.go(CookingLevelSelectorScreen.routePath);\n  }\n  \n  void _navigateBack(BuildContext context) {\n    context.go('/login'); // Or previous step\n  }\n}\n```\n\n---\n\n## 📱 **SHELL NAVIGATION (BOTTOM TABS)**\n\n### **Shell Route Configuration**\n\n```dart\nShellRoute(\n  navigatorKey: _shellNavigatorKey,\n  builder: (context, state, child) {\n    // Update navigation provider when shell builds\n    final currentLocation = state.matchedLocation;\n    \n    if (['/home', '/inventory', '/recipes', '/profile'].contains(currentLocation)) {\n      WidgetsBinding.instance.addPostFrameCallback((_) {\n        if (ref.read(currentNavigationProvider) != currentLocation) {\n          ref.read(currentNavigationProvider.notifier).state = currentLocation;\n        }\n      });\n    }\n    \n    return MainScreen(child: child); // Bottom navigation wrapper\n  },\n  routes: [\n    // Tab 1: Home Dashboard\n    GoRoute(\n      path: '/home',\n      name: homeRouteName,\n      parentNavigatorKey: _shellNavigatorKey,\n      builder: (context, state) => const HomeScreen(),\n    ),\n    \n    // Tab 2: Inventory Management\n    GoRoute(\n      path: '/inventory',\n      name: inventoryRouteName,\n      parentNavigatorKey: _shellNavigatorKey,\n      builder: (context, state) => const InventoryScreen(),\n    ),\n    \n    // Tab 3: Recipe Exploration\n    GoRoute(\n      path: '/recipes',\n      name: recipesRouteName,\n      parentNavigatorKey: _shellNavigatorKey,\n      builder: (context, state) => RecipeScreen(mode: RecipeMode.explore),\n    ),\n    \n    // Tab 4: User Profile\n    GoRoute(\n      path: '/profile',\n      name: profileRouteName,\n      parentNavigatorKey: _shellNavigatorKey,\n      builder: (context, state) => const ProfileScreen(),\n    ),\n    \n    // Nested: Scan Flow (within shell)\n    GoRoute(\n      path: '/scan/add/:itemType',\n      name: addScanItemRouteName,\n      parentNavigatorKey: _shellNavigatorKey,\n      builder: (context, state) {\n        final itemTypeString = state.pathParameters['itemType'];\n        ScanItemType itemType = itemTypeString == 'ingredient' \n            ? ScanItemType.ingredient \n            : ScanItemType.food;\n        return AddScanItemScreen(itemType: itemType);\n      },\n    ),\n  ],\n),\n```\n\n---\n\n## 🎯 **FEATURE-SPECIFIC ROUTES**\n\n### **Full-Screen Feature Routes**\n\n```dart\n// Smart Recipe Generation (No Bottom Bar)\nGoRoute(\n  path: '/smart-recipes',\n  name: smartRecipesRouteName,\n  parentNavigatorKey: _rootNavigatorKey, // Full screen\n  pageBuilder: (context, state) {\n    final mode = state.extra is RecipeMode \n        ? state.extra as RecipeMode \n        : RecipeMode.smartFromInventory;\n    \n    return CustomTransitionPage(\n      key: state.pageKey,\n      child: RecipeScreen(mode: mode),\n      transitionsBuilder: (context, animation, secondaryAnimation, child) {\n        return FadeTransition(opacity: animation, child: child);\n      },\n      transitionDuration: const Duration(milliseconds: 300),\n    );\n  },\n),\n\n// Environmental Impact Panel\nGoRoute(\n  path: '/impact',\n  name: impactRouteName,\n  parentNavigatorKey: _rootNavigatorKey,\n  builder: (context, state) => const ImpactScreen(),\n),\n\n// Weekly Meal Planner\nGoRoute(\n  path: '/planner',\n  name: plannerRouteName,\n  parentNavigatorKey: _rootNavigatorKey,\n  builder: (context, state) => const PlannerScreen(),\n),\n```\n\n### **Parameterized Routes**\n\n```dart\n// Ingredient Detail with Parameter\nGoRoute(\n  path: '/inventory/ingredient/:ingredientName',\n  name: ingredientDetailRouteName,\n  builder: (context, state) {\n    final ingredientName = state.pathParameters['ingredientName'];\n    if (ingredientName == null) {\n      WidgetsBinding.instance.addPostFrameCallback((_) {\n        GoRouter.of(context).go('/inventory');\n      });\n      return const Scaffold(body: Center(child: Text('Ingredient name missing')));\n    }\n    return IngredientDetailScreen(ingredientName: ingredientName);\n  },\n),\n\n// Food Detail with Complex Parameter\nGoRoute(\n  path: '/inventory/food/:itemId',\n  name: foodDetailRouteName,\n  builder: (context, state) {\n    final itemId = state.pathParameters['itemId'];\n    if (itemId == null) {\n      WidgetsBinding.instance.addPostFrameCallback((_) {\n        GoRouter.of(context).go('/inventory');\n      });\n      return const Scaffold(body: Center(child: Text('Item ID missing')));\n    }\n    \n    // Parse composite ID: \"foodName_addedAt\"\n    final parts = itemId.split('_');\n    if (parts.length < 2) {\n      return Scaffold(\n        appBar: AppBar(title: const Text('Error')),\n        body: const Center(child: Text('Invalid food ID format')),\n      );\n    }\n    \n    final foodName = parts[0];\n    final addedAt = parts.sublist(1).join('_');\n    return FoodDetailScreen(foodName: foodName, addedAt: addedAt);\n  },\n),\n```\n\n### **Routes with Extra Data**\n\n```dart\n// Scan Confirmation with Image Data\nGoRoute(\n  path: ScanConfirmScreen.routePath,\n  name: scanConfirmRouteName,\n  builder: (context, state) {\n    final extraData = state.extra as Map<String, dynamic>?;\n    final List<File>? images = extraData?['images'] as List<File>?;\n    final ScanItemType? originType = extraData?['originType'] as ScanItemType?;\n    \n    if (images == null || images.isEmpty || originType == null) {\n      WidgetsBinding.instance.addPostFrameCallback((_) {\n        RouterExtension(context).goNamed('addScanItem', params: {'itemType': 'ingredient'});\n      });\n      return const Scaffold(body: Center(child: CircularProgressIndicator()));\n    }\n    \n    return ScanConfirmScreen(initialImages: images, originType: originType);\n  },\n),\n\n// Recipe Detail with Recipe Data\nGoRoute(\n  path: '/recipes/detail',\n  name: 'recipeDetail',\n  builder: (context, state) {\n    final recipe = state.extra as Map<String, dynamic>?;\n    if (recipe == null) {\n      WidgetsBinding.instance.addPostFrameCallback((_) {\n        GoRouter.of(context).go('/home');\n      });\n      return const Scaffold(body: Center(child: CircularProgressIndicator()));\n    }\n    return RecipeDetailScreen(recipe: recipe);\n  },\n),\n```\n\n---\n\n## 🔄 **NAVIGATION PATTERNS**\n\n### **RouterExtension for Easy Navigation**\n\n```dart\n// File: lib/core/navigation/navigation_extension.dart\nextension RouterExtension on BuildContext {\n  /// Navigate to a named route\n  void goNamed(String name, {Map<String, String> params = const {}}) {\n    GoRouter.of(this).goNamed(name, pathParameters: params);\n  }\n  \n  /// Navigate to a path\n  void go(String path) {\n    GoRouter.of(this).go(path);\n  }\n  \n  /// Replace the current route with a named route\n  void replaceNamed(String name, {Map<String, String> params = const {}}) {\n    GoRouter.of(this).replaceNamed(name, pathParameters: params);\n  }\n  \n  /// Replace the current route with a path\n  void replace(String path) {\n    GoRouter.of(this).replace(path);\n  }\n  \n  /// Push a named route\n  void pushNamed(String name, {Map<String, String> params = const {}, Object? extra}) {\n    GoRouter.of(this).pushNamed(name, pathParameters: params, extra: extra);\n  }\n  \n  /// Go back\n  void pop([Object? result]) {\n    GoRouter.of(this).pop(result);\n  }\n}\n```\n\n### **Navigation Usage Examples**\n\n```dart\n// Basic navigation\ncontext.go('/home');\ncontext.goNamed(homeRouteName);\n\n// Navigation with parameters\ncontext.goNamed(\n  ingredientDetailRouteName, \n  params: {'ingredientName': 'tomato'}\n);\n\n// Navigation with extra data\ncontext.pushNamed(\n  scanConfirmRouteName,\n  extra: {\n    'images': capturedImages,\n    'originType': ScanItemType.ingredient,\n  },\n);\n\n// Replace navigation (no back stack)\ncontext.replaceNamed(homeRouteName);\n\n// Conditional navigation\nif (user.hasCompletedOnboarding) {\n  context.go('/home');\n} else {\n  context.go('/allergy-selector');\n}\n```\n\n---\n\n## 🏠 **BOTTOM NAVIGATION MANAGEMENT**\n\n### **Navigation State Provider**\n\n```dart\n// File: lib/features/navigation/presentation/providers/navigation_provider.dart\nfinal currentNavigationProvider = StateProvider<String>((ref) => '/home');\n\nfinal navigationProvider = StateNotifierProvider<NavigationNotifier, NavigationState>((ref) {\n  return NavigationNotifier();\n});\n\nclass NavigationState {\n  final String currentRoute;\n  final Map<String, dynamic>? routeData;\n  final bool canPop;\n  final List<String> navigationHistory;\n  \n  const NavigationState({\n    required this.currentRoute,\n    this.routeData,\n    this.canPop = false,\n    this.navigationHistory = const [],\n  });\n}\n\nclass NavigationNotifier extends StateNotifier<NavigationState> {\n  NavigationNotifier() : super(const NavigationState(currentRoute: '/home'));\n  \n  void updateCurrentRoute(String route, {Map<String, dynamic>? data}) {\n    state = NavigationState(\n      currentRoute: route,\n      routeData: data,\n      canPop: state.canPop,\n      navigationHistory: [...state.navigationHistory, route],\n    );\n  }\n  \n  void setCanPop(bool canPop) {\n    state = NavigationState(\n      currentRoute: state.currentRoute,\n      routeData: state.routeData,\n      canPop: canPop,\n      navigationHistory: state.navigationHistory,\n    );\n  }\n}\n```\n\n### **Bottom Navigation Implementation**\n\n```dart\n// File: lib/features/navigation/presentation/widgets/app_bottom_app_bar.dart\nclass AppBottomAppBar extends ConsumerWidget {\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final currentRoute = ref.watch(currentNavigationProvider);\n    \n    return BottomAppBar(\n      shape: const CircularNotchedRectangle(),\n      notchMargin: 8.0,\n      child: Row(\n        mainAxisAlignment: MainAxisAlignment.spaceAround,\n        children: [\n          _buildNavItem(context, ref, '/home', Icons.home, 'Home'),\n          _buildNavItem(context, ref, '/inventory', Icons.inventory, 'Inventory'),\n          const SizedBox(width: 48), // Space for FAB\n          _buildNavItem(context, ref, '/recipes', Icons.restaurant, 'Recipes'),\n          _buildNavItem(context, ref, '/profile', Icons.person, 'Profile'),\n        ],\n      ),\n    );\n  }\n  \n  Widget _buildNavItem(BuildContext context, WidgetRef ref, String route, IconData icon, String label) {\n    final currentRoute = ref.watch(currentNavigationProvider);\n    final isSelected = currentRoute == route;\n    \n    return InkWell(\n      onTap: () {\n        if (!isSelected) {\n          context.go(route);\n          ref.read(currentNavigationProvider.notifier).state = route;\n        }\n      },\n      child: Column(\n        mainAxisSize: MainAxisSize.min,\n        children: [\n          Icon(\n            icon,\n            color: isSelected ? Theme.of(context).primaryColor : Colors.grey,\n          ),\n          Text(\n            label,\n            style: TextStyle(\n              fontSize: 12,\n              color: isSelected ? Theme.of(context).primaryColor : Colors.grey,\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}\n```\n\n---\n\n## 🔗 **DEEP LINKING SUPPORT**\n\n### **URL Patterns**\n\n```dart\n// Supported deep link patterns:\n\n// Authentication\n// myapp://login\n// myapp://register\n\n// Inventory\n// myapp://inventory\n// myapp://inventory/ingredient/tomato\n// myapp://inventory/food/pasta_2024-01-15\n\n// Recipes\n// myapp://recipes\n// myapp://recipes/detail?id=recipe_123\n// myapp://smart-recipes\n\n// Profile\n// myapp://profile\n// myapp://profile/allergies\n// myapp://profile/cooking-level\n\n// Impact\n// myapp://impact\n\n// Planner\n// myapp://planner\n// myapp://meal-planning\n```\n\n### **Deep Link Handling**\n\n```dart\n// Router configuration handles deep links automatically\n// GoRouter parses URLs and navigates to appropriate routes\n\n// Example: Handling incoming deep link\nvoid handleDeepLink(String link) {\n  final uri = Uri.parse(link);\n  final path = uri.path;\n  \n  // Router will automatically handle the navigation\n  // based on the configured routes\n}\n```\n\n---\n\n## 🛡️ **NAVIGATION GUARDS**\n\n### **Authentication Guard**\n\n```dart\n// Implemented in router redirect function\nString? authGuard(BuildContext context, GoRouterState state) {\n  final isLoggedIn = ref.watch(authStateProvider).value != null;\n  final protectedRoutes = ['/home', '/inventory', '/recipes', '/profile'];\n  \n  if (!isLoggedIn && protectedRoutes.any((route) => state.matchedLocation.startsWith(route))) {\n    return '/login';\n  }\n  \n  return null; // Allow navigation\n}\n```\n\n### **Onboarding Guard**\n\n```dart\n// Ensure users complete preference setup\nString? onboardingGuard(BuildContext context, GoRouterState state) {\n  final user = ref.watch(userProfileProvider).user;\n  final hasCompletedPreferences = user?.initialPreferencesCompleted ?? false;\n  \n  if (!hasCompletedPreferences && !state.matchedLocation.startsWith('/allergy-selector')) {\n    return '/allergy-selector';\n  }\n  \n  return null;\n}\n```\n\n### **Combined Guard System**\n\n```dart\n// All guards combined in router redirect\nredirect: (context, state) {\n  // Check authentication first\n  final authRedirect = authGuard(context, state);\n  if (authRedirect != null) return authRedirect;\n  \n  // Then check onboarding completion\n  final onboardingRedirect = onboardingGuard(context, state);\n  if (onboardingRedirect != null) return onboardingRedirect;\n  \n  // No redirect needed\n  return null;\n},\n```\n\n---\n\n## 🎨 **CUSTOM TRANSITIONS**\n\n### **Page Transition Animations**\n\n```dart\n// Custom fade transition for smart recipes\nGoRoute(\n  path: '/smart-recipes',\n  pageBuilder: (context, state) {\n    return CustomTransitionPage(\n      key: state.pageKey,\n      child: RecipeScreen(mode: RecipeMode.smartFromInventory),\n      transitionsBuilder: (context, animation, secondaryAnimation, child) {\n        return FadeTransition(opacity: animation, child: child);\n      },\n      transitionDuration: const Duration(milliseconds: 300),\n    );\n  },\n),\n\n// Slide transition for modal-like screens\nGoRoute(\n  path: '/scan/confirm',\n  pageBuilder: (context, state) {\n    return CustomTransitionPage(\n      key: state.pageKey,\n      child: ScanConfirmScreen(),\n      transitionsBuilder: (context, animation, secondaryAnimation, child) {\n        const begin = Offset(0.0, 1.0);\n        const end = Offset.zero;\n        const curve = Curves.easeInOut;\n        \n        var tween = Tween(begin: begin, end: end).chain(\n          CurveTween(curve: curve),\n        );\n        \n        return SlideTransition(\n          position: animation.drive(tween),\n          child: child,\n        );\n      },\n    );\n  },\n),\n```\n\n---\n\n## 🐛 **ERROR HANDLING**\n\n### **Route Error Handling**\n\n```dart\n// Error builder for unknown routes\nerrorBuilder: (context, state) => Scaffold(\n  appBar: AppBar(title: const Text('Page Not Found')),\n  body: Center(\n    child: Column(\n      mainAxisAlignment: MainAxisAlignment.center,\n      children: [\n        const Icon(Icons.error_outline, size: 64, color: Colors.red),\n        const SizedBox(height: 16),\n        Text(\n          'Page not found: ${state.error}',\n          style: Theme.of(context).textTheme.bodyLarge,\n          textAlign: TextAlign.center,\n        ),\n        const SizedBox(height: 24),\n        ElevatedButton(\n          onPressed: () => context.go('/home'),\n          child: const Text('Go Home'),\n        ),\n      ],\n    ),\n  ),\n),\n```\n\n### **Navigation Error Recovery**\n\n```dart\n// Safe navigation with error handling\nextension SafeNavigation on BuildContext {\n  void safeGo(String path) {\n    try {\n      go(path);\n    } catch (e) {\n      // Log error and fallback to home\n      debugPrint('Navigation error: $e');\n      go('/home');\n    }\n  }\n  \n  void safeGoNamed(String name, {Map<String, String> params = const {}}) {\n    try {\n      goNamed(name, params: params);\n    } catch (e) {\n      debugPrint('Navigation error: $e');\n      go('/home');\n    }\n  }\n}\n```\n\nThis comprehensive navigation guide ensures robust, scalable, and user-friendly routing throughout the ZER0 Waste AI application."