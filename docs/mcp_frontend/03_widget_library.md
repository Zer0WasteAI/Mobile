# 🧱 ZER0 WASTE AI - WIDGET LIBRARY\n\n## 📋 **COMPLETE WIDGET CATALOG**\n\nThis document catalogs all widgets in the ZER0 Waste AI application, organized by module and functionality.\n\n---\n\n## 🔧 **CORE WIDGETS**\n\n**Location:** `lib/core/presentation/widgets/`\n\n### **1. BUTTON COMPONENTS**\n\n#### **SmoothButton**\n```dart\n// File: smooth_button.dart\nclass SmoothButton extends StatelessWidget {\n  final String text;\n  final VoidCallback? onPressed;\n  final Color? backgroundColor;\n  final Color? textColor;\n  final double? width;\n  final double? height;\n  final bool isLoading;\n  final IconData? icon;\n  final double borderRadius;\n}\n```\n**Usage:** Primary action buttons throughout the app\n**Features:** Loading state, custom colors, icon support, smooth animations\n\n#### **NextButton** (Onboarding)\n```dart\n// File: features/onboarding/presentation/widgets/next_button.dart\nclass NextButton extends StatelessWidget {\n  final VoidCallback? onPressed;\n  final String text;\n  final bool isEnabled;\n}\n```\n**Usage:** Navigation in onboarding flow\n**Features:** Enabled/disabled states, smooth transitions\n\n### **2. CARD COMPONENTS**\n\n#### **SmoothCard**\n```dart\n// File: smooth_card.dart\nclass SmoothCard extends StatelessWidget {\n  final Widget child;\n  final EdgeInsetsGeometry? padding;\n  final EdgeInsetsGeometry? margin;\n  final Color? backgroundColor;\n  final double borderRadius;\n  final List<BoxShadow>? boxShadow;\n  final VoidCallback? onTap;\n}\n```\n**Usage:** Consistent card styling throughout app\n**Features:** Custom padding, shadows, tap handling, rounded corners\n\n### **3. DIALOG COMPONENTS**\n\n#### **AppDialog**\n```dart\n// File: app_dialog.dart\nclass AppDialog extends StatelessWidget {\n  final String title;\n  final String? content;\n  final Widget? contentWidget;\n  final List<Widget>? actions;\n  final bool barrierDismissible;\n}\n```\n**Usage:** Standard dialog styling\n**Features:** Custom content, action buttons, dismissible behavior\n\n#### **DialogHelper**\n```dart\n// File: dialog_helper.dart\nclass DialogHelper {\n  static Future<T?> showCustomDialog<T>(BuildContext context, Widget dialog)\n  static Future<bool?> showConfirmDialog(BuildContext context, String title, String message)\n  static Future<void> showErrorDialog(BuildContext context, String message)\n  static Future<void> showSuccessDialog(BuildContext context, String message)\n}\n```\n**Usage:** Utility methods for common dialogs\n**Features:** Confirm, error, success dialogs with consistent styling\n\n#### **AddItemDialog**\n```dart\n// File: add_item_dialog.dart\nclass AddItemDialog extends StatefulWidget {\n  final Function(String) onAdd;\n  final String title;\n  final String hintText;\n  final String? initialValue;\n}\n```\n**Usage:** Quick item addition across features\n**Features:** Text input validation, custom titles\n\n### **4. INPUT COMPONENTS**\n\n#### **CustomTextField** (Auth)\n```dart\n// File: features/auth/presentation/widgets/custom_text_field.dart\nclass CustomTextField extends StatelessWidget {\n  final String hintText;\n  final TextEditingController controller;\n  final bool obscureText;\n  final TextInputType keyboardType;\n  final String? Function(String?)? validator;\n  final Widget? suffixIcon;\n  final Widget? prefixIcon;\n}\n```\n**Usage:** Styled text inputs for forms\n**Features:** Validation, icons, password obscuring, keyboard types\n\n#### **UnfocusableSearchField** (Recipes)\n```dart\n// File: features/recipes/presentation/widgets/unfocusable_search_field.dart\nclass UnfocusableSearchField extends StatelessWidget {\n  final String hintText;\n  final Function(String) onChanged;\n  final TextEditingController? controller;\n}\n```\n**Usage:** Search functionality without keyboard focus\n**Features:** Search icon, clear button, custom styling\n\n### **5. SELECTION COMPONENTS**\n\n#### **SelectableItemChip**\n```dart\n// File: selectable_item_chip.dart\nclass SelectableItemChip extends StatelessWidget {\n  final String label;\n  final bool isSelected;\n  final VoidCallback onTap;\n  final Color? selectedColor;\n  final Color? unselectedColor;\n  final IconData? icon;\n}\n```\n**Usage:** Multi-select chips for preferences\n**Features:** Selection states, custom colors, icon support\n\n#### **QuantitySelector** (Scan)\n```dart\n// File: features/scan/presentation/widgets/quantity_selector.dart\nclass QuantitySelector extends StatefulWidget {\n  final double initialQuantity;\n  final String unit;\n  final Function(double) onQuantityChanged;\n  final double min;\n  final double max;\n  final double step;\n}\n```\n**Usage:** Numeric input with increment/decrement\n**Features:** Min/max validation, step increments, unit display\n\n### **6. LOADING COMPONENTS**\n\n#### **LottieLoadingWidget**\n```dart\n// File: lottie_loading_widget.dart\nclass LottieLoadingWidget extends StatelessWidget {\n  final String animationPath;\n  final double? width;\n  final double? height;\n  final String? message;\n  final bool repeat;\n}\n```\n**Usage:** Animated loading indicators\n**Features:** Lottie animations, custom messages, size control\n\n#### **LoadingSnackbar**\n```dart\n// File: loading_snackbar.dart\nclass LoadingSnackbar {\n  static void show(BuildContext context, String message)\n  static void hide(BuildContext context)\n}\n```\n**Usage:** Non-blocking loading feedback\n**Features:** Auto-dismiss, custom messages\n\n### **7. UTILITY COMPONENTS**\n\n#### **UnfocusDetector**\n```dart\n// File: unfocus_detector.dart\nclass UnfocusDetector extends StatelessWidget {\n  final Widget child;\n}\n```\n**Usage:** Dismiss keyboard on tap outside\n**Features:** Automatic keyboard management\n\n#### **SnackbarWrapper**\n```dart\n// File: snackbar_wrapper.dart\nclass SnackbarWrapper {\n  static void showSuccess(BuildContext context, String message)\n  static void showError(BuildContext context, String message)\n  static void showInfo(BuildContext context, String message)\n}\n```\n**Usage:** Consistent snackbar styling\n**Features:** Success, error, info variants with themed colors\n\n---\n\n## 🏠 **HOME MODULE WIDGETS**\n\n**Location:** `lib/features/home/presentation/widgets/`\n\n### **Dashboard Components**\n\n#### **WelcomeHeader**\n```dart\nclass WelcomeHeader extends ConsumerWidget {\n  // Displays personalized greeting with user name\n  // Shows current date and weather-like motivation\n}\n```\n**Features:** User personalization, dynamic greetings, motivational messages\n\n#### **InventorySummaryCard**\n```dart\nclass InventorySummaryCard extends ConsumerWidget {\n  // Shows inventory statistics and quick access\n  // Displays expiring items count and total items\n}\n```\n**Features:** Real-time inventory stats, navigation to inventory, expiration alerts\n\n#### **ImpactSummaryCard**\n```dart\nclass ImpactSummaryCard extends ConsumerWidget {\n  // Environmental impact metrics display\n  // CO2 saved, water saved, eco coins earned\n}\n```\n**Features:** Impact visualization, progress indicators, achievement highlights\n\n#### **QuickActionsWidget**\n```dart\nclass QuickActionsWidget extends StatelessWidget {\n  // 4-button grid for common actions\n  // Scan, Add Item, Generate Recipe, View Impact\n}\n```\n**Features:** Quick navigation, icon-based actions, responsive grid layout\n\n#### **RecipeSuggestions**\n```dart\nclass RecipeSuggestions extends ConsumerWidget {\n  // AI-powered recipe recommendations\n  // Based on current inventory and preferences\n}\n```\n**Features:** Dynamic suggestions, recipe cards, \"Generate More\" action\n\n#### **MotivationalCard**\n```dart\nclass MotivationalCard extends StatelessWidget {\n  // Engagement and educational content\n  // Tips, facts, and encouragement\n}\n```\n**Features:** Rotating content, educational tips, gamification elements\n\n#### **DailyPlannerWidget**\n```dart\nclass DailyPlannerWidget extends ConsumerWidget {\n  // Today's meal plan overview\n  // Breakfast, lunch, dinner preview\n}\n```\n**Features:** Meal plan display, quick edit access, nutritional summary\n\n---\n\n## 📦 **INVENTORY MODULE WIDGETS**\n\n**Location:** `lib/features/inventory/presentation/widgets/`\n\n### **Item Display**\n\n#### **InventoryItemCard**\n```dart\nclass InventoryItemCard extends ConsumerWidget {\n  final InventoryItem item;\n  final VoidCallback? onTap;\n  final VoidCallback? onEdit;\n  final VoidCallback? onDelete;\n  final VoidCallback? onConsume;\n}\n```\n**Features:**\n- Expiration status indicators (color-coded)\n- Quantity and unit display\n- Storage location badges\n- Action buttons (edit, delete, consume)\n- Image display with fallback\n- Nutritional information chips\n\n#### **NutritionInfoChip**\n```dart\nclass NutritionInfoChip extends StatelessWidget {\n  final String label;\n  final String value;\n  final Color? backgroundColor;\n  final IconData? icon;\n}\n```\n**Features:** Compact nutritional data display, themed colors, icon support\n\n### **Filtering & Sorting**\n\n#### **InventoryFilterBottomSheet**\n```dart\nclass InventoryFilterBottomSheet extends ConsumerWidget {\n  // Main filter interface with multiple categories\n  // Category, storage, expiration, quantity filters\n}\n```\n**Features:**\n- Multi-category filtering\n- Reset and apply actions\n- Filter count indicators\n- Persistent filter state\n\n#### **ExpirationFilterBottomSheet**\n```dart\nclass ExpirationFilterBottomSheet extends StatefulWidget {\n  final Function(ExpirationStatus?) onFilterChanged;\n}\n```\n**Features:** Expiration-specific filtering (fresh, expiring, expired)\n\n#### **StorageFilterBottomSheet**\n```dart\nclass StorageFilterBottomSheet extends StatefulWidget {\n  final Function(StorageType?) onFilterChanged;\n}\n```\n**Features:** Storage location filtering (fridge, freezer, pantry, counter)\n\n### **Action Dialogs**\n\n#### **MarkConsumedDialog**\n```dart\nclass MarkConsumedDialog extends StatefulWidget {\n  final InventoryItem item;\n  final Function(double, String?) onConfirm;\n}\n```\n**Features:**\n- Quantity selection for partial consumption\n- Consumption reason selection\n- Recipe association\n- Impact calculation preview\n\n#### **ExpiredItemActionsBottomSheet**\n```dart\nclass ExpiredItemActionsBottomSheet extends StatelessWidget {\n  final InventoryItem item;\n  final VoidCallback onDiscard;\n  final VoidCallback onStillGood;\n  final VoidCallback onCompost;\n}\n```\n**Features:** Specialized actions for expired items, waste tracking\n\n---\n\n## 🍳 **RECIPES MODULE WIDGETS**\n\n**Location:** `lib/features/recipes/presentation/widgets/`\n\n### **Recipe Display**\n\n#### **RecipeCard**\n```dart\nclass RecipeCard extends ConsumerWidget {\n  final Map<String, dynamic> recipe;\n  final VoidCallback? onTap;\n  final bool showFavoriteButton;\n  final bool showEnvironmentalImpact;\n}\n```\n**Features:**\n- Recipe image with fallback\n- Title, description, cooking time\n- Difficulty level indicators\n- Favorite toggle button\n- Environmental impact badges\n- Ingredient availability indicators\n\n#### **FavoriteButton**\n```dart\nclass FavoriteButton extends ConsumerWidget {\n  final Map<String, dynamic> recipe;\n  final double size;\n}\n```\n**Features:** Animated heart icon, save/unsave functionality, visual feedback\n\n#### **RecipeCookingMode**\n```dart\nclass RecipeCookingMode extends ConsumerWidget {\n  final Map<String, dynamic> recipe;\n}\n```\n**Features:**\n- Step-by-step instructions\n- Timer integration\n- Ingredient checklist\n- Progress tracking\n- Voice commands (future)\n- Cooking tips overlay\n\n### **Generation & AI**\n\n#### **AIRecipesSection**\n```dart\nclass AIRecipesSection extends ConsumerWidget {\n  final List<Map<String, dynamic>> recipes;\n  final bool isLoading;\n  final VoidCallback? onGenerateMore;\n}\n```\n**Features:**\n- AI-generated recipe display\n- Loading states with animations\n- \"Generate More\" functionality\n- Inventory utilization indicators\n\n#### **RecipePersonalizationInfoWidget**\n```dart\nclass RecipePersonalizationInfoWidget extends ConsumerWidget {\n  // Shows how recipes are personalized for the user\n  // Dietary preferences, allergies, cooking level\n}\n```\n**Features:** Personalization explanation, preference display, customization options\n\n### **Filtering & Search**\n\n#### **RecipeFilterBottomSheet**\n```dart\nclass RecipeFilterBottomSheet extends ConsumerWidget {\n  final Function(RecipeFilters) onFiltersChanged;\n}\n```\n**Features:**\n- Cuisine type filtering\n- Dietary restriction filters\n- Cooking time ranges\n- Difficulty level selection\n- Ingredient availability filters\n\n#### **RecipeRatingDialog**\n```dart\nclass RecipeRatingDialog extends StatefulWidget {\n  final Map<String, dynamic> recipe;\n  final Function(int, String?) onRatingSubmitted;\n}\n```\n**Features:** 5-star rating system, optional comments, feedback submission\n\n---\n\n## 📷 **SCAN MODULE WIDGETS**\n\n**Location:** `lib/features/scan/presentation/widgets/`\n\n### **Scan Interface**\n\n#### **ScanOptionsModal**\n```dart\nclass ScanOptionsModal extends StatelessWidget {\n  final Function(ScanItemType) onTypeSelected;\n}\n```\n**Features:**\n- Ingredient vs. food selection\n- Visual type indicators\n- Usage examples\n- Quick tips\n\n#### **RecognizedItemCard**\n```dart\nclass RecognizedItemCard extends StatefulWidget {\n  final Map<String, dynamic> item;\n  final Function(Map<String, dynamic>) onItemUpdated;\n  final VoidCallback? onRemove;\n}\n```\n**Features:**\n- Recognition confidence display\n- Editable item details\n- Quantity adjustment\n- Expiration date picker\n- Storage location selection\n- Remove/keep toggles\n\n---\n\n## 🌱 **IMPACT MODULE WIDGETS**\n\n**Location:** `lib/features/impact/presentation/widgets/`\n\n### **Impact Visualization**\n\n#### **ImpactTabBar**\n```dart\nclass ImpactTabBar extends StatelessWidget {\n  final TabController controller;\n}\n```\n**Features:** Custom tab styling, smooth transitions, badge indicators\n\n#### **ImpactDashboardTab**\n```dart\nclass ImpactDashboardTab extends ConsumerWidget {\n  // Current impact metrics and achievements\n}\n```\n**Features:**\n- Real-time metrics display\n- Achievement badges\n- Progress circles\n- Comparison charts\n\n#### **ImpactProgressTab**\n```dart\nclass ImpactProgressTab extends ConsumerWidget {\n  // Historical impact data and trends\n}\n```\n**Features:**\n- Time-series charts\n- Goal tracking\n- Milestone celebrations\n- Trend analysis\n\n#### **ImpactMetricCard**\n```dart\nclass ImpactMetricCard extends StatelessWidget {\n  final String title;\n  final String value;\n  final String unit;\n  final IconData icon;\n  final Color color;\n  final double? progress;\n}\n```\n**Features:** Metric visualization, progress indicators, themed colors\n\n#### **ImpactFactCard**\n```dart\nclass ImpactFactCard extends StatelessWidget {\n  final String fact;\n  final String? source;\n  final IconData? icon;\n}\n```\n**Features:** Educational content, source attribution, engaging visuals\n\n---\n\n## 👤 **PROFILE MODULE WIDGETS**\n\n**Location:** `lib/features/profile/presentation/screens/` (integrated in screens)\n\n### **Preference Selection**\n\nProfile module uses primarily screen-based widgets with integrated selection logic:\n\n- **AllergySelectorScreen** - Multi-select allergy chips\n- **CookingLevelSelectorScreen** - Skill level selection with descriptions\n- **PreferredFoodTypeScreen** - Food category multi-select\n- **SpecialDietSelectorScreen** - Dietary restriction selection\n\nEach uses the core `SelectableItemChip` widget for consistent selection UI.\n\n---\n\n## 🔐 **AUTH MODULE WIDGETS**\n\n**Location:** `lib/features/auth/presentation/widgets/`\n\n### **Authentication Forms**\n\n#### **LoginForm**\n```dart\nclass LoginForm extends ConsumerWidget {\n  // Email/password login form with validation\n}\n```\n**Features:** Form validation, loading states, error display, remember me option\n\n#### **RegisterForm**\n```dart\nclass RegisterForm extends ConsumerWidget {\n  // Registration form with email verification\n}\n```\n**Features:** Password strength validation, terms acceptance, email verification flow\n\n#### **SocialButtonsRow**\n```dart\nclass SocialButtonsRow extends StatelessWidget {\n  final VoidCallback? onGooglePressed;\n  final VoidCallback? onApplePressed;\n}\n```\n**Features:** Social login buttons, platform-specific styling, loading states\n\n### **Specialized Components**\n\n#### **AnimatedLogo**\n```dart\nclass AnimatedLogo extends StatefulWidget {\n  final double size;\n  final Duration duration;\n}\n```\n**Features:** Smooth logo animations, configurable timing, brand consistency\n\n#### **EmailVerificationDialog**\n```dart\nclass EmailVerificationDialog extends StatefulWidget {\n  final String email;\n  final VoidCallback onResendCode;\n  final Function(String) onCodeEntered;\n}\n```\n**Features:** OTP input, resend functionality, countdown timer\n\n#### **OtpInput**\n```dart\nclass OtpInput extends StatefulWidget {\n  final int length;\n  final Function(String) onCompleted;\n}\n```\n**Features:** Multi-digit OTP input, auto-focus progression, paste support\n\n#### **UserInfoDialog**\n```dart\nclass UserInfoDialog extends StatefulWidget {\n  final Function(String, String) onSubmit; // firstName, lastName\n}\n```\n**Features:** Additional user info collection, validation, skip option\n\n---\n\n## 🧭 **NAVIGATION MODULE WIDGETS**\n\n**Location:** `lib/features/navigation/presentation/widgets/`\n\n### **Navigation Components**\n\n#### **AppBottomAppBar**\n```dart\nclass AppBottomAppBar extends ConsumerWidget {\n  final int currentIndex;\n  final Function(int) onTap;\n}\n```\n**Features:**\n- 5-tab navigation (Home, Inventory, Scan, Recipes, Profile)\n- Active state indicators\n- Smooth transitions\n- Badge notifications\n- Floating action button integration\n\n#### **MoreOptionsModal**\n```dart\nclass MoreOptionsModal extends StatelessWidget {\n  // Additional navigation options and quick actions\n}\n```\n**Features:** Extended navigation menu, quick actions, settings access\n\n#### **SafeNavigationButtons**\n```dart\nclass SafeNavigationButtons extends StatelessWidget {\n  final List<NavigationItem> items;\n  final int currentIndex;\n  final Function(int) onTap;\n}\n```\n**Features:** Safe navigation with state preservation, custom styling\n\n---\n\n## 🎨 **WIDGET STYLING SYSTEM**\n\n### **Common Properties**\n\nAll widgets follow consistent styling patterns:\n\n```dart\n// Border Radius\nstatic const double borderRadiusSmall = 8.0;\nstatic const double borderRadiusMedium = 12.0;\nstatic const double borderRadiusLarge = 16.0;\n\n// Spacing\nstatic const double spacingXS = 4.0;\nstatic const double spacingS = 8.0;\nstatic const double spacingM = 16.0;\nstatic const double spacingL = 24.0;\nstatic const double spacingXL = 32.0;\n\n// Elevation\nstatic const double elevationLow = 2.0;\nstatic const double elevationMedium = 4.0;\nstatic const double elevationHigh = 8.0;\n```\n\n### **Animation Standards**\n\n```dart\n// Duration\nstatic const Duration animationFast = Duration(milliseconds: 200);\nstatic const Duration animationMedium = Duration(milliseconds: 300);\nstatic const Duration animationSlow = Duration(milliseconds: 500);\n\n// Curves\nstatic const Curve curveStandard = Curves.easeInOut;\nstatic const Curve curveEmphasized = Curves.easeOutCubic;\n```\n\n### **Responsive Design**\n\nWidgets adapt to different screen sizes using:\n- `MediaQuery` for screen dimensions\n- `LayoutBuilder` for container constraints\n- Flexible and responsive grid systems\n- Adaptive font scaling\n\nThis widget library ensures consistency, reusability, and maintainability across the entire ZER0 Waste AI application."