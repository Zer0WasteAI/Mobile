# 🔄 ZER0 WASTE AI - STATE MANAGEMENT GUIDE\n\n## 📋 **RIVERPOD STATE MANAGEMENT OVERVIEW**\n\nZER0 Waste AI uses **Riverpod** as the primary state management solution, following clean architecture principles and providing reactive, testable state management.\n\n---\n\n## 🏗️ **PROVIDER ARCHITECTURE**\n\n### **Provider Hierarchy**\n\n```\n📱 App Level\n├── 🔐 Authentication Providers\n├── 🧭 Navigation Providers  \n├── 🎨 Theme Providers\n├── 💾 Storage Providers\n│\n📁 Feature Level\n├── 🏠 Home Providers\n├── 📦 Inventory Providers\n├── 🍳 Recipe Providers\n├── 🤖 Recognition Providers\n├── 📷 Scan Providers\n├── 🌱 Impact Providers\n├── 📅 Planner Providers\n└── 👤 Profile Providers\n```\n\n---\n\n## 🔐 **AUTHENTICATION PROVIDERS**\n\n**Location:** `lib/features/auth/presentation/providers/`\n\n### **Core Authentication**\n\n#### **AuthStateProvider**\n```dart\n// File: auth_provider.dart\nfinal authStateProvider = StreamProvider<User?>((ref) {\n  return FirebaseAuth.instance.authStateChanges();\n});\n\nfinal authProvider = StateNotifierProvider<AuthNotifier, AuthState>((ref) {\n  return AuthNotifier(ref.watch(authRepositoryProvider));\n});\n\nclass AuthState {\n  final User? user;\n  final bool isLoading;\n  final String? error;\n  final bool isEmailVerified;\n}\n\nclass AuthNotifier extends StateNotifier<AuthState> {\n  Future<void> signInWithEmailAndPassword(String email, String password)\n  Future<void> registerWithEmailAndPassword(String email, String password)\n  Future<void> signOut()\n  Future<void> sendPasswordResetEmail(String email)\n  Future<void> verifyEmail()\n}\n```\n\n#### **LoginProvider**\n```dart\n// File: login_provider.dart\nfinal loginNotifierProvider = StateNotifierProvider<LoginNotifier, LoginState>((ref) {\n  return LoginNotifier(ref.watch(authRepositoryProvider));\n});\n\nclass LoginState {\n  final bool isLoading;\n  final String? error;\n  final bool isSuccess;\n  final bool rememberMe;\n}\n\nclass LoginNotifier extends StateNotifier<LoginState> {\n  Future<void> login(String email, String password)\n  void toggleRememberMe()\n  void clearError()\n}\n```\n\n#### **RegisterProvider**\n```dart\n// File: register_provider.dart\nfinal registerNotifierProvider = StateNotifierProvider<RegisterNotifier, RegisterState>((ref) {\n  return RegisterNotifier(ref.watch(authRepositoryProvider));\n});\n\nclass RegisterState {\n  final bool isLoading;\n  final String? error;\n  final bool isSuccess;\n  final bool agreeToTerms;\n  final String? emailVerificationSent;\n}\n\nclass RegisterNotifier extends StateNotifier<RegisterState> {\n  Future<void> register(String email, String password, String firstName, String lastName)\n  void toggleTermsAgreement()\n  Future<void> resendVerificationEmail()\n}\n```\n\n#### **ForgotPasswordProvider**\n```dart\n// File: forgot_password_provider.dart\nfinal forgotPasswordProvider = StateNotifierProvider<ForgotPasswordNotifier, ForgotPasswordState>((ref) {\n  return ForgotPasswordNotifier(ref.watch(authRepositoryProvider));\n});\n\nclass ForgotPasswordState {\n  final bool isLoading;\n  final String? error;\n  final bool emailSent;\n}\n\nclass ForgotPasswordNotifier extends StateNotifier<ForgotPasswordState> {\n  Future<void> sendPasswordResetEmail(String email)\n  void reset()\n}\n```\n\n---\n\n## 🧭 **NAVIGATION PROVIDERS**\n\n**Location:** `lib/features/navigation/presentation/providers/`\n\n### **Navigation State**\n\n#### **NavigationProvider**\n```dart\n// File: navigation_provider.dart\nfinal currentNavigationProvider = StateProvider<String>((ref) => '/home');\n\nfinal navigationProvider = StateNotifierProvider<NavigationNotifier, NavigationState>((ref) {\n  return NavigationNotifier();\n});\n\nclass NavigationState {\n  final String currentRoute;\n  final Map<String, dynamic>? routeData;\n  final bool canPop;\n  final List<String> navigationHistory;\n}\n\nclass NavigationNotifier extends StateNotifier<NavigationState> {\n  void navigateTo(String route, {Map<String, dynamic>? data})\n  void goBack()\n  void clearHistory()\n  void updateCurrentRoute(String route)\n}\n```\n\n---\n\n## 🏠 **HOME PROVIDERS**\n\n**Location:** `lib/features/home/application/providers/`\n\n#### **HomeProvider**\n```dart\n// File: home_providers.dart\nfinal homeDataProvider = FutureProvider<HomeData>((ref) async {\n  // Combine data from multiple sources\n  final inventory = await ref.watch(inventoryProvider.future);\n  final recipes = await ref.watch(recentRecipesProvider.future);\n  final impact = await ref.watch(impactSummaryProvider.future);\n  \n  return HomeData(\n    inventoryStats: inventory.stats,\n    recentRecipes: recipes,\n    impactSummary: impact,\n  );\n});\n\nfinal quickActionsProvider = Provider<List<QuickAction>>((ref) {\n  return [\n    QuickAction(title: 'Scan Food', icon: Icons.camera, route: '/scan'),\n    QuickAction(title: 'Add Item', icon: Icons.add, route: '/inventory/add'),\n    QuickAction(title: 'Generate Recipe', icon: Icons.restaurant, route: '/recipes/ai-generation'),\n    QuickAction(title: 'View Impact', icon: Icons.eco, route: '/impact'),\n  ];\n});\n```\n\n---\n\n## 📦 **INVENTORY PROVIDERS**\n\n**Location:** `lib/features/inventory/application/providers/`\n\n### **Main Inventory State**\n\n#### **InventoryProvider**\n```dart\n// File: inventory_provider.dart\nfinal inventoryProvider = StateNotifierProvider<InventoryNotifier, InventoryState>((ref) {\n  return InventoryNotifier(ref.watch(inventoryBackendProvider));\n});\n\nclass InventoryState {\n  final List<InventoryItem> items;\n  final bool isLoading;\n  final String? error;\n  final InventoryFilters filters;\n  final InventorySort sortBy;\n  final InventoryStats stats;\n}\n\nclass InventoryNotifier extends StateNotifier<InventoryState> {\n  Future<void> loadInventory()\n  Future<void> addItem(InventoryItem item)\n  Future<void> updateItem(String id, InventoryItem item)\n  Future<void> deleteItem(String id)\n  Future<void> markAsConsumed(String id, double quantity, String? reason)\n  void applyFilters(InventoryFilters filters)\n  void updateSort(InventorySort sortBy)\n  Future<void> refreshStats()\n}\n```\n\n#### **InventoryBackendProvider**\n```dart\n// File: inventory_backend_provider.dart\nfinal inventoryBackendProvider = Provider<InventoryBackendProvider>((ref) {\n  return InventoryBackendProvider(ref.watch(inventoryRepositoryProvider));\n});\n\nclass InventoryBackendProvider {\n  Future<List<InventoryItem>> getInventory()\n  Future<InventoryItem> addItem(Map<String, dynamic> itemData)\n  Future<InventoryItem> updateItem(String id, Map<String, dynamic> updates)\n  Future<void> deleteItem(String id)\n  Future<List<InventoryItem>> getExpiringItems(int days)\n  Future<InventoryStats> getInventoryStats()\n  Future<InventoryItem> markItemConsumed(String id, double quantity, String? reason)\n}\n```\n\n### **Filtering and Sorting**\n\n#### **Filter Providers**\n```dart\n// Inventory filters\nfinal inventoryFiltersProvider = StateProvider<InventoryFilters>((ref) {\n  return const InventoryFilters();\n});\n\nfinal filteredInventoryProvider = Provider<List<InventoryItem>>((ref) {\n  final items = ref.watch(inventoryProvider).items;\n  final filters = ref.watch(inventoryFiltersProvider);\n  \n  return _applyFilters(items, filters);\n});\n\nclass InventoryFilters {\n  final ItemCategory? category;\n  final StorageType? storageType;\n  final ExpirationStatus? expirationStatus;\n  final double? minQuantity;\n  final double? maxQuantity;\n  final String? searchQuery;\n}\n```\n\n---\n\n## 🍳 **RECIPE PROVIDERS**\n\n**Location:** `lib/features/recipes/application/providers/`\n\n### **Recipe Generation**\n\n#### **AIRecipeProvider**\n```dart\n// File: ai_recipes_provider.dart\nfinal aiRecipeProvider = StateNotifierProvider<AIRecipeNotifier, RecipeState>((ref) {\n  return AIRecipeNotifier(ref.watch(recipeBackendProvider));\n});\n\nclass RecipeState {\n  final List<Map<String, dynamic>> recipes;\n  final bool isLoading;\n  final String? error;\n  final bool hasMore;\n  final RecipeGenerationMode mode;\n}\n\nclass AIRecipeNotifier extends StateNotifier<RecipeState> {\n  Future<void> generateRecipesFromInventory()\n  Future<void> generateCustomRecipes(List<String> ingredients, RecipePreferences preferences)\n  Future<void> loadMoreRecipes()\n  void clearRecipes()\n  void setGenerationMode(RecipeGenerationMode mode)\n}\n```\n\n#### **RecipeBackendProvider**\n```dart\n// File: recipe_backend_provider.dart\nfinal recipeBackendProvider = Provider<RecipeBackendProvider>((ref) {\n  return RecipeBackendProvider(ref.watch(recipeRepositoryProvider));\n});\n\nclass RecipeBackendProvider {\n  Future<Map<String, dynamic>> generateRecipesFromInventory()\n  Future<Map<String, dynamic>> generateCustomRecipes(List<String> ingredients, RecipePreferences preferences)\n  Future<List<Map<String, dynamic>>> getAllRecipes()\n  Future<List<Map<String, dynamic>>> getSavedRecipes()\n  Future<void> saveRecipe(Map<String, dynamic> recipe)\n  Future<void> deleteRecipe(String recipeId)\n}\n```\n\n### **Recipe Management**\n\n#### **FavoriteRecipesProvider**\n```dart\n// File: favorite_recipes_provider.dart\nfinal favoriteRecipesProvider = StateNotifierProvider<FavoriteRecipesNotifier, List<Map<String, dynamic>>>((ref) {\n  return FavoriteRecipesNotifier(ref.watch(recipeBackendProvider));\n});\n\nclass FavoriteRecipesNotifier extends StateNotifier<List<Map<String, dynamic>>> {\n  Future<void> loadFavorites()\n  Future<void> addToFavorites(Map<String, dynamic> recipe)\n  Future<void> removeFromFavorites(String recipeId)\n  bool isFavorite(String recipeId)\n}\n```\n\n#### **RecipeProvider** (General)\n```dart\n// File: recipe_providers.dart\nfinal allRecipesProvider = FutureProvider<List<Map<String, dynamic>>>((ref) async {\n  final backend = ref.watch(recipeBackendProvider);\n  return await backend.getAllRecipes();\n});\n\nfinal recipeFiltersProvider = StateProvider<RecipeFilters>((ref) {\n  return const RecipeFilters();\n});\n\nfinal filteredRecipesProvider = Provider<AsyncValue<List<Map<String, dynamic>>>>((ref) {\n  final recipesAsync = ref.watch(allRecipesProvider);\n  final filters = ref.watch(recipeFiltersProvider);\n  \n  return recipesAsync.when(\n    data: (recipes) => AsyncValue.data(_applyRecipeFilters(recipes, filters)),\n    loading: () => const AsyncValue.loading(),\n    error: (error, stack) => AsyncValue.error(error, stack),\n  );\n});\n```\n\n---\n\n## 🤖 **RECOGNITION PROVIDERS**\n\n**Location:** `lib/features/recognition/presentation/providers/`\n\n### **Recognition Processing**\n\n#### **SimplifiedRecognitionProvider**\n```dart\n// File: simplified_recognition_provider.dart\nfinal simplifiedRecognitionProvider = StateNotifierProvider<SimplifiedRecognitionNotifier, SimplifiedRecognitionState>((ref) {\n  return SimplifiedRecognitionNotifier(ref.watch(recognitionRepositoryProvider));\n});\n\nclass SimplifiedRecognitionState {\n  final List<Map<String, dynamic>> recognizedItems;\n  final bool isProcessing;\n  final String? error;\n  final double progress;\n  final String? currentStep;\n  final String? recognitionId;\n}\n\nclass SimplifiedRecognitionNotifier extends StateNotifier<SimplifiedRecognitionState> {\n  Future<void> recognizeIngredients(List<File> images)\n  Future<void> checkImageStatus(String recognitionId)\n  Future<void> updateItemDetails(int index, Map<String, dynamic> updates)\n  void removeItem(int index)\n  void clearResults()\n}\n```\n\n#### **SimplifiedFoodRecognitionProvider**\n```dart\n// File: simplified_food_recognition_provider.dart\nfinal simplifiedFoodRecognitionProvider = StateNotifierProvider<SimplifiedFoodRecognitionNotifier, SimplifiedFoodRecognitionState>((ref) {\n  return SimplifiedFoodRecognitionNotifier(ref.watch(recognitionRepositoryProvider));\n});\n\nclass SimplifiedFoodRecognitionState {\n  final List<Map<String, dynamic>> recognizedFoods;\n  final bool isProcessing;\n  final String? error;\n  final double progress;\n  final String? currentStep;\n  final String? recognitionId;\n}\n\nclass SimplifiedFoodRecognitionNotifier extends StateNotifier<SimplifiedFoodRecognitionState> {\n  Future<void> recognizeFoods(List<File> images)\n  Future<void> checkImageStatus(String recognitionId)\n  Future<void> updateFoodDetails(int index, Map<String, dynamic> updates)\n  void removeFood(int index)\n  void clearResults()\n}\n```\n\n---\n\n## 📷 **SCAN PROVIDERS**\n\n**Location:** `lib/features/scan/application/providers/` & `lib/features/scan/presentation/providers/`\n\n### **Scan Results Management**\n\n#### **ScanResultsProvider**\n```dart\n// File: scan_results_provider.dart\nfinal scanResultsProvider = StateNotifierProvider<ScanResultsNotifier, ScanResultsState>((ref) {\n  return ScanResultsNotifier();\n});\n\nclass ScanResultsState {\n  final List<Map<String, dynamic>> items;\n  final bool isLoading;\n  final String? error;\n  final ScanItemType scanType;\n}\n\nclass ScanResultsNotifier extends StateNotifier<ScanResultsState> {\n  void setResults(List<Map<String, dynamic>> items, ScanItemType type)\n  void updateItem(int index, Map<String, dynamic> updates)\n  void removeItem(int index)\n  void addItem(Map<String, dynamic> item)\n  Future<void> saveToInventory()\n}\n```\n\n#### **AddScanItemProvider**\n```dart\n// File: add_scan_item_provider.dart\nfinal addScanItemProvider = StateNotifierProvider<AddScanItemNotifier, AddScanItemState>((ref) {\n  return AddScanItemNotifier();\n});\n\nclass AddScanItemState {\n  final String name;\n  final double quantity;\n  final String unit;\n  final DateTime? expirationDate;\n  final StorageType storageType;\n  final bool isLoading;\n  final String? error;\n}\n\nclass AddScanItemNotifier extends StateNotifier<AddScanItemState> {\n  void updateName(String name)\n  void updateQuantity(double quantity)\n  void updateUnit(String unit)\n  void updateExpirationDate(DateTime? date)\n  void updateStorageType(StorageType type)\n  Future<void> saveItem()\n}\n```\n\n---\n\n## 🌱 **IMPACT PROVIDERS**\n\n**Location:** `lib/features/impact/application/providers/`\n\n#### **ImpactProvider**\n```dart\n// File: impact_providers.dart\nfinal impactProvider = StateNotifierProvider<ImpactNotifier, ImpactState>((ref) {\n  return ImpactNotifier(ref.watch(impactRepositoryProvider));\n});\n\nclass ImpactState {\n  final EnvironmentalSummary summary;\n  final List<ImpactMetric> metrics;\n  final List<ImpactCalculation> calculations;\n  final bool isLoading;\n  final String? error;\n}\n\nclass ImpactNotifier extends StateNotifier<ImpactState> {\n  Future<void> loadImpactSummary()\n  Future<void> loadCalculations()\n  Future<void> calculateRecipeImpact(String recipeUid)\n  Future<void> updateCalculationStatus(String recipeUid, bool isCooked)\n  Future<void> refreshData()\n}\n\nfinal impactSummaryProvider = FutureProvider<EnvironmentalSummary>((ref) async {\n  final repository = ref.watch(impactRepositoryProvider);\n  return await repository.getImpactSummary();\n});\n\nfinal impactMetricsProvider = FutureProvider<List<ImpactMetric>>((ref) async {\n  final summary = await ref.watch(impactSummaryProvider.future);\n  return [\n    ImpactMetric(title: 'CO2 Saved', value: summary.co2Saved, unit: 'kg'),\n    ImpactMetric(title: 'Water Saved', value: summary.waterSaved, unit: 'L'),\n    ImpactMetric(title: 'Food Saved', value: summary.foodSaved, unit: 'kg'),\n    ImpactMetric(title: 'Money Saved', value: summary.moneySaved, unit: '\$'),\n  ];\n});\n```\n\n---\n\n## 📅 **PLANNER PROVIDERS**\n\n**Location:** `lib/features/planner/application/providers/`\n\n#### **MealPlanProvider**\n```dart\n// File: meal_plan_providers.dart\nfinal mealPlanProvider = StateNotifierProvider<MealPlanNotifier, MealPlanState>((ref) {\n  return MealPlanNotifier(ref.watch(mealPlanRepositoryProvider));\n});\n\nclass MealPlanState {\n  final Map<String, MealPlan> weeklyPlans; // Date -> MealPlan\n  final DateTime currentWeek;\n  final bool isLoading;\n  final String? error;\n}\n\nclass MealPlanNotifier extends StateNotifier<MealPlanState> {\n  Future<void> loadWeeklyPlans(DateTime weekStart)\n  Future<void> saveMealPlan(String date, MealPlan plan)\n  Future<void> updateMealPlan(String date, MealPlan plan)\n  Future<void> deleteMealPlan(String date)\n  Future<void> generateMealPlan(DateTime weekStart)\n  void navigateToWeek(DateTime weekStart)\n}\n```\n\n#### **MealPlanningProvider**\n```dart\n// File: meal_planning_providers.dart\nfinal mealPlanningProvider = StateNotifierProvider<MealPlanningNotifier, MealPlanningState>((ref) {\n  return MealPlanningNotifier(ref.watch(mealPlanRepositoryProvider));\n});\n\nclass MealPlanningState {\n  final MealPlan? currentPlan;\n  final List<Map<String, dynamic>> availableRecipes;\n  final bool isGenerating;\n  final String? error;\n}\n\nclass MealPlanningNotifier extends StateNotifier<MealPlanningState> {\n  Future<void> createNewPlan(String date)\n  Future<void> generateAIPlan(List<Map<String, dynamic>> ingredients)\n  void assignRecipeToMeal(MealType mealType, Map<String, dynamic> recipe)\n  void removeRecipeFromMeal(MealType mealType)\n  Future<void> savePlan()\n}\n```\n\n---\n\n## 👤 **PROFILE PROVIDERS**\n\n**Location:** `lib/features/profile/application/providers/`\n\n### **User Profile Management**\n\n#### **UserProfileProvider**\n```dart\n// File: user_profile_provider.dart\nfinal userProfileProvider = StateNotifierProvider<UserProfileNotifier, UserProfileState>((ref) {\n  return UserProfileNotifier();\n});\n\nclass UserProfileState {\n  final UserEntity? user;\n  final bool isLoading;\n  final String? error;\n  final bool hasCompletedOnboarding;\n}\n\nclass UserProfileNotifier extends StateNotifier<UserProfileState> {\n  Future<void> loadUserProfile()\n  Future<void> updateProfile(UserEntity user)\n  Future<void> completeOnboarding()\n  void setUser(UserEntity user)\n}\n```\n\n### **Preference Providers**\n\n#### **AllergiesProvider**\n```dart\n// File: allergies_provider.dart\nfinal allergiesProvider = StateNotifierProvider<AllergiesNotifier, List<Allergy>>((ref) {\n  return AllergiesNotifier();\n});\n\nfinal selectedAllergiesProvider = StateNotifierProvider<SelectedAllergiesNotifier, List<String>>((ref) {\n  return SelectedAllergiesNotifier();\n});\n\nclass AllergiesNotifier extends StateNotifier<List<Allergy>> {\n  Future<void> loadAllergies()\n}\n\nclass SelectedAllergiesNotifier extends StateNotifier<List<String>> {\n  void toggleAllergy(String allergyId)\n  void setAllergies(List<String> allergyIds)\n  Future<void> saveAllergies()\n}\n```\n\n#### **SpecialDietsProvider**\n```dart\n// File: special_diets_provider.dart\nfinal specialDietsProvider = StateNotifierProvider<SpecialDietsNotifier, List<SpecialDiet>>((ref) {\n  return SpecialDietsNotifier();\n});\n\nfinal selectedSpecialDietsProvider = StateNotifierProvider<SelectedSpecialDietsNotifier, List<String>>((ref) {\n  return SelectedSpecialDietsNotifier();\n});\n\nclass SpecialDietsNotifier extends StateNotifier<List<SpecialDiet>> {\n  Future<void> loadSpecialDiets()\n}\n\nclass SelectedSpecialDietsNotifier extends StateNotifier<List<String>> {\n  void toggleDiet(String dietId)\n  void setDiets(List<String> dietIds)\n  Future<void> saveDiets()\n}\n```\n\n#### **FoodTypesProvider**\n```dart\n// File: food_types_provider.dart\nfinal foodTypesProvider = StateNotifierProvider<FoodTypesNotifier, List<FoodType>>((ref) {\n  return FoodTypesNotifier();\n});\n\nfinal selectedFoodTypesProvider = StateNotifierProvider<SelectedFoodTypesNotifier, List<String>>((ref) {\n  return SelectedFoodTypesNotifier();\n});\n\nclass FoodTypesNotifier extends StateNotifier<List<FoodType>> {\n  Future<void> loadFoodTypes()\n}\n\nclass SelectedFoodTypesNotifier extends StateNotifier<List<String>> {\n  void toggleFoodType(String foodTypeId)\n  void setFoodTypes(List<String> foodTypeIds)\n  Future<void> saveFoodTypes()\n}\n```\n\n---\n\n## 🎨 **THEME & UI PROVIDERS**\n\n**Location:** `lib/core/theme/`\n\n#### **ThemeProvider**\n```dart\n// File: theme.dart\nfinal themeProvider = StateProvider<ThemeMode>((ref) => ThemeMode.system);\n\n// Extension for easy theme access\nextension ThemeExtension on WidgetRef {\n  ThemeData get theme => Theme.of(context);\n  bool get isDarkMode => theme.brightness == Brightness.dark;\n  \n  void toggleTheme() {\n    final currentMode = read(themeProvider);\n    final newMode = currentMode == ThemeMode.light \n        ? ThemeMode.dark \n        : ThemeMode.light;\n    read(themeProvider.notifier).state = newMode;\n  }\n}\n```\n\n---\n\n## 💾 **STORAGE PROVIDERS**\n\n**Location:** `lib/core/local_storage/`\n\n#### **Storage Providers**\n```dart\n// Hive boxes\nfinal settingsBoxProvider = Provider<Box>((ref) {\n  return Hive.box('settings');\n});\n\nfinal userBoxProvider = Provider<Box>((ref) {\n  return Hive.box('user');\n});\n\n// SharedPreferences\nfinal sharedPrefsProvider = Provider<SharedPreferencesHelper>((ref) {\n  throw UnimplementedError('SharedPreferences must be overridden in main.dart');\n});\n\n// Secure Storage\nfinal secureStorageProvider = Provider<FlutterSecureStorage>((ref) {\n  return const FlutterSecureStorage();\n});\n```\n\n---\n\n## 🔄 **PROVIDER PATTERNS & BEST PRACTICES**\n\n### **1. State Composition**\n\n```dart\n// Combining multiple providers for complex state\nfinal dashboardDataProvider = FutureProvider<DashboardData>((ref) async {\n  final inventory = await ref.watch(inventoryStatsProvider.future);\n  final recipes = await ref.watch(recentRecipesProvider.future);\n  final impact = await ref.watch(impactSummaryProvider.future);\n  \n  return DashboardData(\n    inventoryStats: inventory,\n    recentRecipes: recipes,\n    impactSummary: impact,\n  );\n});\n```\n\n### **2. Error Handling**\n\n```dart\n// Consistent error handling pattern\nclass BaseNotifier<T> extends StateNotifier<AsyncValue<T>> {\n  BaseNotifier() : super(const AsyncValue.loading());\n  \n  Future<void> executeWithErrorHandling(Future<T> Function() operation) async {\n    try {\n      state = const AsyncValue.loading();\n      final result = await operation();\n      state = AsyncValue.data(result);\n    } catch (error, stackTrace) {\n      state = AsyncValue.error(error, stackTrace);\n    }\n  }\n}\n```\n\n### **3. Provider Dependencies**\n\n```dart\n// Proper dependency injection\nfinal recipeNotifierProvider = StateNotifierProvider<RecipeNotifier, RecipeState>((ref) {\n  return RecipeNotifier(\n    repository: ref.watch(recipeRepositoryProvider),\n    authState: ref.watch(authStateProvider),\n  );\n});\n```\n\n### **4. State Persistence**\n\n```dart\n// Persisting state across app restarts\nfinal persistentSettingsProvider = StateNotifierProvider<PersistentSettingsNotifier, Settings>((ref) {\n  final box = ref.watch(settingsBoxProvider);\n  return PersistentSettingsNotifier(box);\n});\n\nclass PersistentSettingsNotifier extends StateNotifier<Settings> {\n  final Box _box;\n  \n  PersistentSettingsNotifier(this._box) : super(Settings.fromJson(_box.get('settings', defaultValue: {})));\n  \n  @override\n  set state(Settings value) {\n    super.state = value;\n    _box.put('settings', value.toJson());\n  }\n}\n```\n\n### **5. Provider Scoping**\n\n```dart\n// Feature-scoped providers\nfinal inventoryScope = Provider<ProviderContainer>((ref) {\n  return ProviderContainer(\n    parent: ref.container,\n    overrides: [\n      // Feature-specific overrides\n    ],\n  );\n});\n```\n\n---\n\n## 🧪 **TESTING PROVIDERS**\n\n### **Provider Testing Pattern**\n\n```dart\n// Testing providers with mocks\nvoid main() {\n  group('InventoryNotifier', () {\n    late ProviderContainer container;\n    late MockInventoryRepository mockRepository;\n    \n    setUp(() {\n      mockRepository = MockInventoryRepository();\n      container = ProviderContainer(\n        overrides: [\n          inventoryRepositoryProvider.overrideWithValue(mockRepository),\n        ],\n      );\n    });\n    \n    test('should load inventory successfully', () async {\n      when(() => mockRepository.getInventory())\n          .thenAnswer((_) async => []);\n      \n      final notifier = container.read(inventoryProvider.notifier);\n      await notifier.loadInventory();\n      \n      expect(container.read(inventoryProvider).items, isEmpty);\n      expect(container.read(inventoryProvider).isLoading, false);\n    });\n  });\n}\n```\n\nThis comprehensive state management guide ensures consistent, scalable, and maintainable state handling across the entire ZER0 Waste AI application using Riverpod's powerful provider system."