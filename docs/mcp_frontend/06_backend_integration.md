# 🔌 ZER0 WASTE AI - BACKEND INTEGRATION GUIDE\n\n## 📋 **BACKEND INTEGRATION OVERVIEW**\n\nZER0 Waste AI integrates with a **MCP (Model Context Protocol) backend** that provides AI-powered food recognition, recipe generation, and environmental impact tracking.\n\n---\n\n## 🏗️ **INTEGRATION ARCHITECTURE**\n\n### **Service Layer Structure**\n\n```\n📁 lib/core/services/\n├── 🌐 api_service.dart           # Main API service (68+ endpoints)\n├── 🔐 secure_token_service.dart  # JWT token management\n└── 💾 storage_service.dart       # Local data persistence\n\n📁 lib/core/network/\n└── 🔒 auth_interceptor.dart      # Automatic token refresh\n```\n\n---\n\n## 🌐 **API SERVICE CONFIGURATION**\n\n**Location:** `lib/core/services/api_service.dart`\n\n### **Service Initialization**\n\n```dart\nclass ApiService {\n  static ApiService? _instance;\n  static ApiService get instance => _instance ??= ApiService._internal();\n  \n  late final Dio _dio;\n  static const String _accessTokenKey = 'access_token';\n  static const String _refreshTokenKey = 'refresh_token';\n  \n  // Standardized timeout constants\n  static const Duration _aiProcessingTimeout = Duration(minutes: 3);\n  static const Duration _uploadTimeout = Duration(minutes: 2);\n  static const Duration _standardTimeout = Duration(seconds: 30);\n  \n  ApiService._internal() {\n    final baseUrl = dotenv.env['BACKEND_BASE_URL'] ?? 'http://127.0.0.1:3000';\n    \n    _dio = Dio(BaseOptions(\n      baseUrl: baseUrl,\n      connectTimeout: _standardTimeout,\n      receiveTimeout: _standardTimeout,\n      sendTimeout: _uploadTimeout,\n      headers: {\n        'Content-Type': 'application/json',\n        'Accept': 'application/json',\n      },\n    ));\n    \n    // Add interceptors\n    _dio.interceptors.add(LogInterceptor(requestBody: true, responseBody: true));\n  }\n}\n```\n\n### **Environment Configuration**\n\n```dart\n// .env file configuration\nBACKEND_BASE_URL=http://127.0.0.1:3000\nFIREBASE_API_KEY=your_firebase_key\nFIREBASE_PROJECT_ID=your_project_id\n```\n\n---\n\n## 🔐 **AUTHENTICATION INTEGRATION**\n\n### **JWT Token Management**\n\n```dart\n// Authentication endpoints\nstatic const String _authFirebaseSignin = '/api/auth/firebase-signin';\nstatic const String _authRefresh = '/api/auth/refresh';\nstatic const String _authLogout = '/api/auth/logout';\n\n/// Firebase authentication with backend JWT exchange\nFuture<Map<String, dynamic>> firebaseSignIn(String firebaseIdToken) async {\n  try {\n    final response = await _dio.post(\n      _authFirebaseSignin,\n      data: {'firebase_id_token': firebaseIdToken},\n      options: Options(receiveTimeout: _standardTimeout),\n    );\n    \n    // Store JWT tokens\n    final accessToken = response.data['access_token'];\n    final refreshToken = response.data['refresh_token'];\n    \n    await _secureStorage.write(key: _accessTokenKey, value: accessToken);\n    await _secureStorage.write(key: _refreshTokenKey, value: refreshToken);\n    \n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Firebase sign-in error: ${e.toString()}');\n  }\n}\n\n/// Refresh JWT tokens\nFuture<Map<String, dynamic>> refreshTokens() async {\n  try {\n    final refreshToken = await _secureStorage.read(key: _refreshTokenKey);\n    if (refreshToken == null) throw Exception('No refresh token available');\n    \n    final response = await _dio.post(\n      _authRefresh,\n      data: {'refresh_token': refreshToken},\n    );\n    \n    // Update stored tokens\n    final newAccessToken = response.data['access_token'];\n    await _secureStorage.write(key: _accessTokenKey, value: newAccessToken);\n    \n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Token refresh error: ${e.toString()}');\n  }\n}\n```\n\n### **Automatic Token Refresh**\n\n**Location:** `lib/core/network/auth_interceptor.dart`\n\n```dart\nclass AuthInterceptor extends Interceptor {\n  final FlutterSecureStorage _secureStorage;\n  final AuthRepository _authRepository;\n  final Dio _dio;\n  final FirebaseAuth _firebaseAuth;\n  final ApiService _apiService;\n  \n  @override\n  void onRequest(RequestOptions options, RequestInterceptorHandler handler) async {\n    // Add authorization header\n    final token = await _secureStorage.read(key: 'access_token');\n    if (token != null) {\n      options.headers['Authorization'] = 'Bearer $token';\n    }\n    handler.next(options);\n  }\n  \n  @override\n  void onError(DioException err, ErrorInterceptorHandler handler) async {\n    if (err.response?.statusCode == 401) {\n      // Token expired, attempt refresh\n      try {\n        await _apiService.refreshTokens();\n        \n        // Retry original request with new token\n        final newToken = await _secureStorage.read(key: 'access_token');\n        err.requestOptions.headers['Authorization'] = 'Bearer $newToken';\n        \n        final response = await _dio.fetch(err.requestOptions);\n        handler.resolve(response);\n        return;\n      } catch (refreshError) {\n        // Refresh failed, logout user\n        await _authRepository.signOut();\n        handler.next(err);\n        return;\n      }\n    }\n    handler.next(err);\n  }\n}\n```\n\n---\n\n## 👤 **USER PROFILE INTEGRATION**\n\n### **Profile Management**\n\n```dart\n// User profile endpoints\nstatic const String _userProfile = '/api/user/profile';\n\n/// Get user profile\nFuture<Map<String, dynamic>> getProfile() async {\n  try {\n    final response = await _dio.get(_userProfile);\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get profile error: ${e.toString()}');\n  }\n}\n\n/// Update user profile\nFuture<Map<String, dynamic>> updateProfile(Map<String, dynamic> profileData) async {\n  try {\n    final response = await _dio.put(\n      _userProfile,\n      data: profileData,\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Update profile error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 🤖 **AI RECOGNITION INTEGRATION**\n\n### **Food & Ingredient Recognition**\n\n```dart\n// Recognition endpoints\nstatic const String _recognitionFoods = '/api/recognition/foods';\nstatic const String _recognitionIngredients = '/api/recognition/ingredients';\nstatic const String _recognitionIngredientsAsync = '/api/recognition/ingredients/async';\nstatic const String _recognitionStatus = '/api/recognition/status';\n\n/// AI recognition of prepared foods/dishes\nFuture<Map<String, dynamic>> recognizeFoods(List<String> imagePaths) async {\n  try {\n    final response = await _dio.post(\n      _recognitionFoods,\n      data: {'images_paths': imagePaths},\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Food recognition error: ${e.toString()}');\n  }\n}\n\n/// AI recognition of ingredients\nFuture<Map<String, dynamic>> recognizeIngredients(List<String> imagePaths) async {\n  try {\n    final response = await _dio.post(\n      _recognitionIngredients,\n      data: {'images_paths': imagePaths},\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Ingredient recognition error: ${e.toString()}');\n  }\n}\n\n/// Asynchronous ingredient recognition\nFuture<Map<String, dynamic>> recognizeIngredientsAsync(List<String> imagePaths) async {\n  try {\n    final response = await _dio.post(\n      _recognitionIngredientsAsync,\n      data: {'images_paths': imagePaths},\n      options: Options(\n        receiveTimeout: _standardTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Async ingredient recognition error: ${e.toString()}');\n  }\n}\n\n/// Check recognition status\nFuture<Map<String, dynamic>> getRecognitionStatus(String taskId) async {\n  try {\n    final response = await _dio.get('$_recognitionStatus/$taskId');\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Recognition status error: ${e.toString()}');\n  }\n}\n```\n\n### **Image Upload Integration**\n\n```dart\n// Image upload endpoints\nstatic const String _imagesUpload = '/api/images/upload';\nstatic const String _imagesUploadInventory = '/api/images/upload/inventory';\n\n/// Upload image to backend\nFuture<Map<String, dynamic>> uploadImage(File imageFile) async {\n  try {\n    final formData = FormData.fromMap({\n      'image': await MultipartFile.fromFile(\n        imageFile.path,\n        filename: path.basename(imageFile.path),\n      ),\n    });\n    \n    final response = await _dio.post(\n      _imagesUpload,\n      data: formData,\n      options: Options(\n        receiveTimeout: _uploadTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    \n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Image upload error: ${e.toString()}');\n  }\n}\n\n/// Upload inventory item image\nFuture<Map<String, dynamic>> uploadInventoryImage(File imageFile) async {\n  try {\n    final formData = FormData.fromMap({\n      'image': await MultipartFile.fromFile(\n        imageFile.path,\n        filename: path.basename(imageFile.path),\n      ),\n    });\n    \n    final response = await _dio.post(\n      _imagesUploadInventory,\n      data: formData,\n      options: Options(\n        receiveTimeout: _uploadTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    \n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Inventory image upload error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 📦 **INVENTORY INTEGRATION**\n\n### **Inventory Management**\n\n```dart\n// Inventory endpoints\nstatic const String _inventory = '/api/inventory';\nstatic const String _inventoryIngredients = '/api/inventory/ingredients';\nstatic const String _inventoryFoods = '/api/inventory/foods';\nstatic const String _inventoryAddItem = '/api/inventory/add_item';\n\n/// Get complete inventory\nFuture<Map<String, dynamic>> getInventory() async {\n  try {\n    final response = await _dio.get(_inventory);\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get inventory error: ${e.toString()}');\n  }\n}\n\n/// Add multiple ingredients to inventory (bulk operation)\nFuture<Map<String, dynamic>> addIngredients(List<Map<String, dynamic>> ingredients) async {\n  try {\n    final response = await _dio.post(\n      _inventoryIngredients,\n      data: {'ingredients': ingredients},\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Add ingredients error: ${e.toString()}');\n  }\n}\n\n/// Add single item to inventory\nFuture<Map<String, dynamic>> addInventoryItem(Map<String, dynamic> item) async {\n  try {\n    final response = await _dio.post(\n      _inventory,\n      data: item,\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Add inventory item error: ${e.toString()}');\n  }\n}\n\n/// Update ingredient expiration date\nFuture<Map<String, dynamic>> updateIngredientExpirationDate(\n  String ingredientName,\n  String newExpirationDate,\n) async {\n  try {\n    final encodedName = Uri.encodeComponent(ingredientName);\n    final response = await _dio.put(\n      '$_inventoryIngredients/$encodedName/expiration',\n      data: {'expiration_date': newExpirationDate},\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Update ingredient expiration error: ${e.toString()}');\n  }\n}\n\n/// Mark item as consumed\nFuture<Map<String, dynamic>> markItemConsumed(\n  String itemId,\n  Map<String, dynamic> consumptionData,\n) async {\n  try {\n    final response = await _dio.post(\n      '$_inventory/$itemId/consume',\n      data: consumptionData,\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Mark consumed error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 🍳 **RECIPE INTEGRATION**\n\n### **AI Recipe Generation**\n\n```dart\n// Recipe endpoints\nstatic const String _recipesGenerateFromInventory = '/api/recipes/generate-from-inventory';\nstatic const String _recipesGenerateCustom = '/api/recipes/generate-custom';\nstatic const String _recipesGenerate = '/api/recipes/generate';\nstatic const String _recipesSaved = '/api/recipes/saved';\n\n/// Generate recipes from current inventory\nFuture<Map<String, dynamic>> generateRecipesFromInventory() async {\n  try {\n    final response = await _dio.post(\n      _recipesGenerateFromInventory,\n      data: {},\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Generate recipes from inventory error: ${e.toString()}');\n  }\n}\n\n/// Generate custom recipes with specific ingredients\nFuture<Map<String, dynamic>> generateCustomRecipes(\n  List<String> ingredients,\n  Map<String, dynamic>? preferences,\n  List<String>? recipeCategories,\n  int numRecipes,\n) async {\n  try {\n    final response = await _dio.post(\n      _recipesGenerateCustom,\n      data: {\n        'ingredients': ingredients,\n        if (preferences != null) 'preferences': preferences,\n        if (recipeCategories != null) 'recipe_categories': recipeCategories,\n        'num_recipes': numRecipes,\n      },\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Generate custom recipes error: ${e.toString()}');\n  }\n}\n\n/// Generate recipe with specific ingredients list (used by inventory module)\nFuture<Map<String, dynamic>> generateRecipe(List<Map<String, dynamic>> ingredients) async {\n  try {\n    final response = await _dio.post(\n      _recipesGenerate,\n      data: {'ingredients': ingredients},\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Generate recipe error: ${e.toString()}');\n  }\n}\n\n/// Get saved recipes\nFuture<Map<String, dynamic>> getSavedRecipes() async {\n  try {\n    final response = await _dio.get(_recipesSaved);\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get saved recipes error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 🌱 **ENVIRONMENTAL IMPACT INTEGRATION**\n\n### **Impact Tracking**\n\n```dart\n// Impact endpoints\nstatic const String _impactSummary = '/api/impact/summary';\nstatic const String _impactCalculate = '/api/impact/calculate';\nstatic const String _impactCalculations = '/api/impact/calculations';\n\n/// Get environmental impact summary\nFuture<Map<String, dynamic>> getImpactSummary() async {\n  try {\n    final response = await _dio.get(_impactSummary);\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get impact summary error: ${e.toString()}');\n  }\n}\n\n/// Calculate recipe environmental impact\nFuture<Map<String, dynamic>> calculateRecipeImpact(String recipeUid) async {\n  try {\n    final response = await _dio.post(\n      _impactCalculate,\n      data: {'recipe_uid': recipeUid},\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Calculate recipe impact error: ${e.toString()}');\n  }\n}\n\n/// Get impact calculations\nFuture<Map<String, dynamic>> getImpactCalculations() async {\n  try {\n    final response = await _dio.get(_impactCalculations);\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get impact calculations error: ${e.toString()}');\n  }\n}\n\n/// Update calculation status\nFuture<Map<String, dynamic>> updateCalculationStatus(\n  String recipeUid,\n  bool isCooked,\n) async {\n  try {\n    final response = await _dio.put(\n      '$_impactCalculations/$recipeUid/status',\n      data: {'is_cooked': isCooked},\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Update calculation status error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 📅 **MEAL PLANNING INTEGRATION**\n\n### **Meal Plan Management**\n\n```dart\n// Planning endpoints\nstatic const String _planGenerate = '/api/planning/generate';\nstatic const String _planWeekly = '/api/planning/weekly';\nstatic const String _planningDelete = '/api/planning';\n\n/// Generate meal plan from available ingredients\nFuture<Map<String, dynamic>> generateMealPlan(List<Map<String, dynamic>> ingredients) async {\n  try {\n    final response = await _dio.post(\n      _planGenerate,\n      data: {'ingredients': ingredients},\n      options: Options(\n        receiveTimeout: _aiProcessingTimeout,\n        sendTimeout: _uploadTimeout,\n      ),\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Generate meal plan error: ${e.toString()}');\n  }\n}\n\n/// Get weekly meal plan\nFuture<Map<String, dynamic>> getWeeklyMealPlan(String startDate, String endDate) async {\n  try {\n    final response = await _dio.get(\n      _planWeekly,\n      queryParameters: {\n        'start_date': startDate,\n        'end_date': endDate,\n      },\n    );\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Get weekly meal plan error: ${e.toString()}');\n  }\n}\n\n/// Delete meal plan for specific date\nFuture<Map<String, dynamic>> deleteMealPlan(String date) async {\n  try {\n    final response = await _dio.delete('$_planningDelete/$date');\n    return response.data as Map<String, dynamic>;\n  } catch (e) {\n    throw Exception('Delete meal plan error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 🛠️ **ERROR HANDLING**\n\n### **Centralized Error Management**\n\n```dart\n// Custom exception classes\nclass ApiException implements Exception {\n  final String message;\n  final int? statusCode;\n  final dynamic data;\n  \n  ApiException(this.message, {this.statusCode, this.data});\n  \n  @override\n  String toString() => 'ApiException: $message (Status: $statusCode)';\n}\n\nclass NetworkException extends ApiException {\n  NetworkException(String message) : super(message);\n}\n\nclass AuthenticationException extends ApiException {\n  AuthenticationException(String message) : super(message, statusCode: 401);\n}\n\n// Error handling utility\nclass ApiErrorHandler {\n  static Exception handleDioError(DioException error) {\n    switch (error.type) {\n      case DioExceptionType.connectionTimeout:\n      case DioExceptionType.sendTimeout:\n      case DioExceptionType.receiveTimeout:\n        return NetworkException('Connection timeout. Please check your internet connection.');\n      \n      case DioExceptionType.badResponse:\n        final statusCode = error.response?.statusCode;\n        final message = error.response?.data?['message'] ?? 'Unknown error occurred';\n        \n        switch (statusCode) {\n          case 401:\n            return AuthenticationException('Authentication failed. Please log in again.');\n          case 403:\n            return ApiException('Access forbidden.', statusCode: statusCode);\n          case 404:\n            return ApiException('Resource not found.', statusCode: statusCode);\n          case 500:\n            return ApiException('Server error. Please try again later.', statusCode: statusCode);\n          default:\n            return ApiException(message, statusCode: statusCode);\n        }\n      \n      case DioExceptionType.cancel:\n        return ApiException('Request was cancelled.');\n      \n      default:\n        return NetworkException('Network error. Please check your connection.');\n    }\n  }\n}\n```\n\n---\n\n## 📊 **RESPONSE HANDLING**\n\n### **Standardized Response Processing**\n\n```dart\n// Response wrapper for consistent handling\nclass ApiResponse<T> {\n  final bool success;\n  final T? data;\n  final String? message;\n  final int? statusCode;\n  \n  ApiResponse({\n    required this.success,\n    this.data,\n    this.message,\n    this.statusCode,\n  });\n  \n  factory ApiResponse.success(T data, {String? message}) {\n    return ApiResponse(\n      success: true,\n      data: data,\n      message: message,\n    );\n  }\n  \n  factory ApiResponse.error(String message, {int? statusCode}) {\n    return ApiResponse(\n      success: false,\n      message: message,\n      statusCode: statusCode,\n    );\n  }\n}\n\n// Usage in API methods\nFuture<ApiResponse<Map<String, dynamic>>> safeApiCall(\n  Future<Response> Function() apiCall,\n) async {\n  try {\n    final response = await apiCall();\n    return ApiResponse.success(\n      response.data as Map<String, dynamic>,\n      message: 'Request completed successfully',\n    );\n  } on DioException catch (e) {\n    final exception = ApiErrorHandler.handleDioError(e);\n    return ApiResponse.error(\n      exception.toString(),\n      statusCode: e.response?.statusCode,\n    );\n  } catch (e) {\n    return ApiResponse.error('Unexpected error: ${e.toString()}');\n  }\n}\n```\n\n---\n\n## 🔧 **TESTING INTEGRATION**\n\n### **API Service Testing**\n\n```dart\n// Mock API service for testing\nclass MockApiService extends ApiService {\n  final Map<String, dynamic> _mockResponses = {};\n  \n  void setMockResponse(String endpoint, Map<String, dynamic> response) {\n    _mockResponses[endpoint] = response;\n  }\n  \n  @override\n  Future<Map<String, dynamic>> getInventory() async {\n    await Future.delayed(const Duration(milliseconds: 100)); // Simulate network delay\n    return _mockResponses['/api/inventory'] ?? {'items': []};\n  }\n  \n  @override\n  Future<Map<String, dynamic>> recognizeIngredients(List<String> imagePaths) async {\n    await Future.delayed(const Duration(seconds: 2)); // Simulate AI processing\n    return _mockResponses['/api/recognition/ingredients'] ?? {\n      'recognized_items': [],\n      'confidence': 0.95,\n    };\n  }\n}\n\n// Integration tests\nvoid main() {\n  group('API Integration Tests', () {\n    late MockApiService mockApiService;\n    \n    setUp(() {\n      mockApiService = MockApiService();\n    });\n    \n    test('should fetch inventory successfully', () async {\n      // Setup mock response\n      mockApiService.setMockResponse('/api/inventory', {\n        'items': [\n          {'name': 'Tomato', 'quantity': 5, 'unit': 'pieces'},\n        ],\n      });\n      \n      // Test API call\n      final result = await mockApiService.getInventory();\n      \n      // Verify result\n      expect(result['items'], isNotEmpty);\n      expect(result['items'][0]['name'], equals('Tomato'));\n    });\n  });\n}\n```\n\nThis comprehensive backend integration guide ensures robust, scalable, and maintainable communication between the ZER0 Waste AI frontend and the MCP backend system."