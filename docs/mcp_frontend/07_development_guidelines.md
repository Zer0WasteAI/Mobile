# 🛠️ ZER0 WASTE AI - DEVELOPMENT GUIDELINES\n\n## 📋 **DEVELOPMENT STANDARDS & BEST PRACTICES**\n\nThis guide establishes coding standards, patterns, and best practices for ZER0 Waste AI development.\n\n---\n\n## 🎯 **CODING STANDARDS**\n\n### **File Naming Conventions**\n\n```dart\n// Files and directories: snake_case\nuser_profile_screen.dart\ninventory_item_card.dart\napi_service.dart\n\n// Classes: PascalCase\nclass UserProfileScreen extends ConsumerWidget {}\nclass InventoryItemCard extends StatelessWidget {}\nclass ApiService {}\n\n// Variables and functions: camelCase\nString userName;\nvoid updateUserProfile() {}\nfinal inventoryProvider = StateNotifierProvider<>;\n\n// Constants: SCREAMING_SNAKE_CASE\nstatic const String API_BASE_URL = 'https://api.example.com';\nstatic const Duration DEFAULT_TIMEOUT = Duration(seconds: 30);\n```\n\n### **Import Organization**\n\n```dart\n// 1. Dart core libraries\nimport 'dart:async';\nimport 'dart:io';\n\n// 2. Flutter framework\nimport 'package:flutter/material.dart';\nimport 'package:flutter/services.dart';\n\n// 3. Third-party packages (alphabetical)\nimport 'package:dio/dio.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\nimport 'package:go_router/go_router.dart';\n\n// 4. Internal imports (alphabetical)\nimport 'package:zer0_waste_ai/core/theme/app_colors.dart';\nimport 'package:zer0_waste_ai/features/auth/domain/entities/user_entity.dart';\nimport 'package:zer0_waste_ai/features/inventory/presentation/widgets/inventory_item_card.dart';\n```\n\n---\n\n## 🏗️ **ARCHITECTURE PATTERNS**\n\n### **Clean Architecture Implementation**\n\n```dart\n// Domain Layer - Business Logic\nclass GetInventoryUseCase {\n  final InventoryRepository repository;\n  \n  GetInventoryUseCase(this.repository);\n  \n  Future<Either<Failure, List<InventoryItem>>> call() async {\n    return await repository.getInventory();\n  }\n}\n\n// Data Layer - External Data Sources\nclass InventoryRepositoryImpl implements InventoryRepository {\n  final ApiService apiService;\n  final LocalStorageService localStorage;\n  \n  InventoryRepositoryImpl(this.apiService, this.localStorage);\n  \n  @override\n  Future<Either<Failure, List<InventoryItem>>> getInventory() async {\n    try {\n      final response = await apiService.getInventory();\n      final items = response.map((json) => InventoryItem.fromJson(json)).toList();\n      await localStorage.cacheInventory(items);\n      return Right(items);\n    } catch (e) {\n      return Left(ServerFailure(e.toString()));\n    }\n  }\n}\n\n// Presentation Layer - UI State Management\nclass InventoryNotifier extends StateNotifier<InventoryState> {\n  final GetInventoryUseCase getInventoryUseCase;\n  \n  InventoryNotifier(this.getInventoryUseCase) : super(const InventoryState.initial());\n  \n  Future<void> loadInventory() async {\n    state = state.copyWith(isLoading: true);\n    \n    final result = await getInventoryUseCase();\n    \n    result.fold(\n      (failure) => state = state.copyWith(\n        isLoading: false,\n        error: failure.message,\n      ),\n      (items) => state = state.copyWith(\n        isLoading: false,\n        items: items,\n        error: null,\n      ),\n    );\n  }\n}\n```\n\n### **State Management Patterns**\n\n```dart\n// 1. Use Freezed for immutable state classes\n@freezed\nclass InventoryState with _$InventoryState {\n  const factory InventoryState({\n    @Default([]) List<InventoryItem> items,\n    @Default(false) bool isLoading,\n    String? error,\n    @Default(InventoryFilters()) InventoryFilters filters,\n  }) = _InventoryState;\n}\n\n// 2. Provider dependency injection\nfinal inventoryRepositoryProvider = Provider<InventoryRepository>((ref) {\n  return InventoryRepositoryImpl(\n    ref.watch(apiServiceProvider),\n    ref.watch(localStorageProvider),\n  );\n});\n\n// 3. Feature-scoped providers\nfinal inventoryProvider = StateNotifierProvider<InventoryNotifier, InventoryState>((ref) {\n  return InventoryNotifier(\n    GetInventoryUseCase(ref.watch(inventoryRepositoryProvider)),\n  );\n});\n```\n\n---\n\n## 🧱 **WIDGET DEVELOPMENT**\n\n### **Widget Structure Guidelines**\n\n```dart\n// 1. Use ConsumerWidget for Riverpod integration\nclass InventoryItemCard extends ConsumerWidget {\n  final InventoryItem item;\n  final VoidCallback? onTap;\n  final VoidCallback? onEdit;\n  final VoidCallback? onDelete;\n  \n  const InventoryItemCard({\n    super.key,\n    required this.item,\n    this.onTap,\n    this.onEdit,\n    this.onDelete,\n  });\n  \n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final theme = Theme.of(context);\n    \n    return Card(\n      elevation: 2,\n      margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),\n      child: InkWell(\n        onTap: onTap,\n        borderRadius: BorderRadius.circular(12),\n        child: Padding(\n          padding: const EdgeInsets.all(16),\n          child: _buildContent(context, theme),\n        ),\n      ),\n    );\n  }\n  \n  Widget _buildContent(BuildContext context, ThemeData theme) {\n    return Column(\n      crossAxisAlignment: CrossAxisAlignment.start,\n      children: [\n        _buildHeader(theme),\n        const SizedBox(height: 8),\n        _buildDetails(theme),\n        const SizedBox(height: 12),\n        _buildActions(),\n      ],\n    );\n  }\n  \n  Widget _buildHeader(ThemeData theme) {\n    return Row(\n      children: [\n        _buildItemImage(),\n        const SizedBox(width: 12),\n        Expanded(\n          child: Column(\n            crossAxisAlignment: CrossAxisAlignment.start,\n            children: [\n              Text(\n                item.name,\n                style: theme.textTheme.titleMedium?.copyWith(\n                  fontWeight: FontWeight.bold,\n                ),\n              ),\n              Text(\n                '${item.quantity} ${item.unit}',\n                style: theme.textTheme.bodyMedium?.copyWith(\n                  color: theme.colorScheme.onSurfaceVariant,\n                ),\n              ),\n            ],\n          ),\n        ),\n        _buildExpirationBadge(theme),\n      ],\n    );\n  }\n  \n  // Additional private methods...\n}\n```\n\n### **Responsive Design Patterns**\n\n```dart\n// 1. Use LayoutBuilder for responsive layouts\nclass ResponsiveInventoryGrid extends StatelessWidget {\n  final List<InventoryItem> items;\n  \n  const ResponsiveInventoryGrid({super.key, required this.items});\n  \n  @override\n  Widget build(BuildContext context) {\n    return LayoutBuilder(\n      builder: (context, constraints) {\n        final crossAxisCount = _calculateCrossAxisCount(constraints.maxWidth);\n        \n        return GridView.builder(\n          gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(\n            crossAxisCount: crossAxisCount,\n            crossAxisSpacing: 16,\n            mainAxisSpacing: 16,\n            childAspectRatio: 0.8,\n          ),\n          itemCount: items.length,\n          itemBuilder: (context, index) => InventoryItemCard(item: items[index]),\n        );\n      },\n    );\n  }\n  \n  int _calculateCrossAxisCount(double width) {\n    if (width < 600) return 1;      // Mobile\n    if (width < 900) return 2;      // Tablet portrait\n    if (width < 1200) return 3;     // Tablet landscape\n    return 4;                       // Desktop\n  }\n}\n\n// 2. Use MediaQuery for screen-specific adjustments\nclass AdaptiveAppBar extends StatelessWidget implements PreferredSizeWidget {\n  final String title;\n  final List<Widget>? actions;\n  \n  const AdaptiveAppBar({super.key, required this.title, this.actions});\n  \n  @override\n  Widget build(BuildContext context) {\n    final isMobile = MediaQuery.of(context).size.width < 600;\n    \n    return AppBar(\n      title: Text(title),\n      actions: actions,\n      elevation: isMobile ? 1 : 0,\n      centerTitle: isMobile,\n    );\n  }\n  \n  @override\n  Size get preferredSize => const Size.fromHeight(kToolbarHeight);\n}\n```\n\n---\n\n## 🔄 **ERROR HANDLING**\n\n### **Comprehensive Error Management**\n\n```dart\n// 1. Define custom exceptions\nabstract class AppException implements Exception {\n  final String message;\n  final String? code;\n  \n  const AppException(this.message, {this.code});\n}\n\nclass NetworkException extends AppException {\n  const NetworkException(String message) : super(message, code: 'NETWORK_ERROR');\n}\n\nclass AuthenticationException extends AppException {\n  const AuthenticationException(String message) : super(message, code: 'AUTH_ERROR');\n}\n\nclass ValidationException extends AppException {\n  const ValidationException(String message) : super(message, code: 'VALIDATION_ERROR');\n}\n\n// 2. Error handling in providers\nclass InventoryNotifier extends StateNotifier<AsyncValue<List<InventoryItem>>> {\n  final InventoryRepository repository;\n  \n  InventoryNotifier(this.repository) : super(const AsyncValue.loading());\n  \n  Future<void> loadInventory() async {\n    state = const AsyncValue.loading();\n    \n    try {\n      final items = await repository.getInventory();\n      state = AsyncValue.data(items);\n    } on NetworkException catch (e) {\n      state = AsyncValue.error(\n        'Network error: ${e.message}',\n        StackTrace.current,\n      );\n    } on AuthenticationException catch (e) {\n      state = AsyncValue.error(\n        'Authentication error: ${e.message}',\n        StackTrace.current,\n      );\n    } catch (e, stackTrace) {\n      state = AsyncValue.error(\n        'Unexpected error: ${e.toString()}',\n        stackTrace,\n      );\n    }\n  }\n}\n\n// 3. Error display in UI\nclass ErrorDisplay extends StatelessWidget {\n  final String message;\n  final VoidCallback? onRetry;\n  \n  const ErrorDisplay({super.key, required this.message, this.onRetry});\n  \n  @override\n  Widget build(BuildContext context) {\n    return Center(\n      child: Padding(\n        padding: const EdgeInsets.all(24),\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            const Icon(\n              Icons.error_outline,\n              size: 64,\n              color: Colors.red,\n            ),\n            const SizedBox(height: 16),\n            Text(\n              message,\n              textAlign: TextAlign.center,\n              style: Theme.of(context).textTheme.bodyLarge,\n            ),\n            if (onRetry != null) ..[\n              const SizedBox(height: 24),\n              ElevatedButton(\n                onPressed: onRetry,\n                child: const Text('Retry'),\n              ),\n            ],\n          ],\n        ),\n      ),\n    );\n  }\n}\n```\n\n---\n\n## 🧪 **TESTING GUIDELINES**\n\n### **Unit Testing Patterns**\n\n```dart\n// 1. Provider testing\nvoid main() {\n  group('InventoryNotifier', () {\n    late ProviderContainer container;\n    late MockInventoryRepository mockRepository;\n    \n    setUp(() {\n      mockRepository = MockInventoryRepository();\n      container = ProviderContainer(\n        overrides: [\n          inventoryRepositoryProvider.overrideWithValue(mockRepository),\n        ],\n      );\n    });\n    \n    tearDown(() {\n      container.dispose();\n    });\n    \n    test('should load inventory successfully', () async {\n      // Arrange\n      final mockItems = [InventoryItem(name: 'Test', quantity: 1, unit: 'piece')];\n      when(() => mockRepository.getInventory())\n          .thenAnswer((_) async => mockItems);\n      \n      // Act\n      final notifier = container.read(inventoryProvider.notifier);\n      await notifier.loadInventory();\n      \n      // Assert\n      final state = container.read(inventoryProvider);\n      expect(state.hasValue, true);\n      expect(state.value, equals(mockItems));\n    });\n    \n    test('should handle network error', () async {\n      // Arrange\n      when(() => mockRepository.getInventory())\n          .thenThrow(const NetworkException('Connection failed'));\n      \n      // Act\n      final notifier = container.read(inventoryProvider.notifier);\n      await notifier.loadInventory();\n      \n      // Assert\n      final state = container.read(inventoryProvider);\n      expect(state.hasError, true);\n      expect(state.error.toString(), contains('Network error'));\n    });\n  });\n}\n```\n\n### **Widget Testing Patterns**\n\n```dart\nvoid main() {\n  group('InventoryItemCard', () {\n    testWidgets('should display item information', (tester) async {\n      // Arrange\n      const item = InventoryItem(\n        name: 'Tomato',\n        quantity: 5,\n        unit: 'pieces',\n        expirationDate: '2024-12-31',\n      );\n      \n      // Act\n      await tester.pumpWidget(\n        MaterialApp(\n          home: Scaffold(\n            body: InventoryItemCard(item: item),\n          ),\n        ),\n      );\n      \n      // Assert\n      expect(find.text('Tomato'), findsOneWidget);\n      expect(find.text('5 pieces'), findsOneWidget);\n    });\n    \n    testWidgets('should call onTap when tapped', (tester) async {\n      // Arrange\n      bool wasTapped = false;\n      const item = InventoryItem(name: 'Test', quantity: 1, unit: 'piece');\n      \n      // Act\n      await tester.pumpWidget(\n        MaterialApp(\n          home: Scaffold(\n            body: InventoryItemCard(\n              item: item,\n              onTap: () => wasTapped = true,\n            ),\n          ),\n        ),\n      );\n      \n      await tester.tap(find.byType(InventoryItemCard));\n      \n      // Assert\n      expect(wasTapped, true);\n    });\n  });\n}\n```\n\n---\n\n## 📱 **PERFORMANCE OPTIMIZATION**\n\n### **Widget Performance**\n\n```dart\n// 1. Use const constructors\nclass StaticWidget extends StatelessWidget {\n  const StaticWidget({super.key}); // Always use const\n  \n  @override\n  Widget build(BuildContext context) {\n    return const Text('Static content'); // Const for static content\n  }\n}\n\n// 2. Optimize list rendering\nclass OptimizedInventoryList extends ConsumerWidget {\n  const OptimizedInventoryList({super.key});\n  \n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final inventoryAsync = ref.watch(inventoryProvider);\n    \n    return inventoryAsync.when(\n      data: (items) => ListView.builder(\n        itemCount: items.length,\n        itemBuilder: (context, index) {\n          final item = items[index];\n          return InventoryItemCard(\n            key: ValueKey(item.id), // Stable keys for performance\n            item: item,\n          );\n        },\n      ),\n      loading: () => const Center(child: CircularProgressIndicator()),\n      error: (error, stack) => ErrorDisplay(message: error.toString()),\n    );\n  }\n}\n\n// 3. Memoization for expensive operations\nclass ExpensiveCalculationWidget extends ConsumerWidget {\n  const ExpensiveCalculationWidget({super.key});\n  \n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final data = ref.watch(dataProvider);\n    final expensiveResult = useMemoized(\n      () => performExpensiveCalculation(data),\n      [data],\n    );\n    \n    return Text('Result: $expensiveResult');\n  }\n}\n```\n\n### **State Management Performance**\n\n```dart\n// 1. Provider scoping\nfinal inventoryFiltersProvider = StateProvider<InventoryFilters>((ref) {\n  return const InventoryFilters();\n});\n\n// 2. Selective watching\nclass FilteredInventoryWidget extends ConsumerWidget {\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    // Only watch what you need\n    final items = ref.watch(inventoryProvider.select((state) => state.items));\n    final filters = ref.watch(inventoryFiltersProvider);\n    \n    final filteredItems = items.where((item) => filters.matches(item)).toList();\n    \n    return ListView.builder(\n      itemCount: filteredItems.length,\n      itemBuilder: (context, index) => InventoryItemCard(item: filteredItems[index]),\n    );\n  }\n}\n\n// 3. Debounced operations\nclass SearchNotifier extends StateNotifier<String> {\n  SearchNotifier() : super('');\n  \n  Timer? _debounceTimer;\n  \n  void updateQuery(String query) {\n    _debounceTimer?.cancel();\n    _debounceTimer = Timer(const Duration(milliseconds: 300), () {\n      state = query;\n    });\n  }\n  \n  @override\n  void dispose() {\n    _debounceTimer?.cancel();\n    super.dispose();\n  }\n}\n```\n\n---\n\n## 🔒 **SECURITY BEST PRACTICES**\n\n### **Data Protection**\n\n```dart\n// 1. Secure token storage\nclass SecureTokenService {\n  static const FlutterSecureStorage _storage = FlutterSecureStorage(\n    aOptions: AndroidOptions(\n      encryptedSharedPreferences: true,\n    ),\n    iOptions: IOSOptions(\n      accessibility: KeychainAccessibility.first_unlock_this_device,\n    ),\n  );\n  \n  static Future<void> storeToken(String key, String token) async {\n    await _storage.write(key: key, value: token);\n  }\n  \n  static Future<String?> getToken(String key) async {\n    return await _storage.read(key: key);\n  }\n  \n  static Future<void> deleteToken(String key) async {\n    await _storage.delete(key: key);\n  }\n}\n\n// 2. Input validation\nclass ValidationUtils {\n  static String? validateEmail(String? email) {\n    if (email == null || email.isEmpty) {\n      return 'Email is required';\n    }\n    \n    final emailRegex = RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$');\n    if (!emailRegex.hasMatch(email)) {\n      return 'Please enter a valid email';\n    }\n    \n    return null;\n  }\n  \n  static String? validatePassword(String? password) {\n    if (password == null || password.isEmpty) {\n      return 'Password is required';\n    }\n    \n    if (password.length < 8) {\n      return 'Password must be at least 8 characters';\n    }\n    \n    if (!RegExp(r'^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)').hasMatch(password)) {\n      return 'Password must contain uppercase, lowercase, and number';\n    }\n    \n    return null;\n  }\n}\n\n// 3. Safe API calls\nclass SecureApiService {\n  Future<Map<String, dynamic>> makeSecureRequest(\n    String endpoint,\n    Map<String, dynamic> data,\n  ) async {\n    // Sanitize input data\n    final sanitizedData = _sanitizeInput(data);\n    \n    // Add security headers\n    final headers = {\n      'Content-Type': 'application/json',\n      'X-Requested-With': 'XMLHttpRequest',\n      'X-Client-Version': await _getAppVersion(),\n    };\n    \n    // Make request with timeout\n    final response = await _dio.post(\n      endpoint,\n      data: sanitizedData,\n      options: Options(\n        headers: headers,\n        receiveTimeout: const Duration(seconds: 30),\n      ),\n    );\n    \n    return response.data as Map<String, dynamic>;\n  }\n  \n  Map<String, dynamic> _sanitizeInput(Map<String, dynamic> data) {\n    // Remove potentially dangerous characters\n    return data.map((key, value) {\n      if (value is String) {\n        return MapEntry(key, value.replaceAll(RegExp(r'[<>\"\\']'), ''));\n      }\n      return MapEntry(key, value);\n    });\n  }\n}\n```\n\nThese development guidelines ensure consistent, maintainable, and high-quality code across the ZER0 Waste AI project."